# Retired pre–v2-onboard live rules (2026-06-28)

YAML snapshots replaced when **CABINET / HOME_MIRROR / SOFA / OTTOMAN** were
recertified through the v2_onboard promote pipeline.

| File | Replaced by |
| --- | --- |
| `*.yaml` here | `../live_eligible_2026-06-28/` content re-staged with version `*_v2_onboarded_2026-06-28` |

Production canonical rules are again `mode: live_eligible` under
`api_attribute_rules/*.yaml`.

Do not copy these back without an explicit rollback runbook.
