# Project Status

## 当前阶段
- **阶段**：商品全生命周期运营系统 Phase 1-3 实施完成
- **里程碑**：关键词研究、竞品分析、广告管理、利润核算、库存规划、每日巡检、每周报告、商品生命周期状态机全部落地

## 最新进展
- ✅ **2026-06-25 / Cursor**: Confidence Review Pipeline 已生产部署 — 镜像升级为 `amz-listing-management-system:2026-06-25-confidence-review-pipeline`，主容器 healthy，3 个 scheduler running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302，`list-categories` 正常。已按 `docs/proposals/confidence-review-pipeline-dev-plan.md` 落地 evidence-grounded 分级审核闭环：新增 `ConfidenceScorer`、`review_policy.yaml`、`AttributeReviewAgent`、`ReviewManager`、`AttributeReviewPlanRouter`、`AmazonListingPendingReviewRepository` 与 Alembic `010_amz_listing_pending_review`；`AttributeResolver` 输出 `review_status/review_route/confidence_score/review_context` 并支持 overrides，required LLM 不再在 resolver 内硬写 `blocking=True`；CoverageGate 仅放行 `auto_approved/completed`，未审核 required LLM 返回 `NEEDS_REVIEW_REQUIRED_ATTRIBUTE`；plan builder 将纯审核阻断转为 `needs_review` 落库，混合缺必填仍 fail-closed；CLI 新增 `review-pending-attributes` / `submit-reviewed-plans`。生产迁移：Alembic `010_amz_listing_pending_review` 已执行，新表存在。生产验收：review CLI 与 submit reviewed dry-run 正常；临时 smoke review 记录完成“落库 -> AI 审核 -> completed -> 清理”闭环；CHAIR 单 SKU `meow2511081Gqqd` dry-run 未 PUT，LLM 提取成功，最终仍因真实非审核类缺口 `blocked_attribute_coverage` fail-closed；主容器近期日志无 JSON 解析失败/Traceback/ERROR。验证：targeted tests 51 passed，新模块覆盖率总 85%，改动文件 ruff 通过，全量 unit+integration 回归通过；报告见 `docs/test-reports/2026-06-25-confidence-review-pipeline-dev.md`。
- ✅ **2026-06-25 / Cursor**: S1 Amazon Schema 深合并已生产部署 — 镜像升级为 `amz-listing-management-system:2026-06-25-schema-deep-merge`，主容器 healthy，3 个 scheduler running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302，`list-categories` 正常。`AmazonSchemaService._merged_properties` 从浅 `dict.update` 改为递归深合并，避免 `allOf[].then/else` 的部分条件约束把 root `items.properties` 整键覆盖；`required` 列表取并集，`enum/examples` 等非白名单 list 不盲目 union。新增 schema merge golden 夹具（17 个 cached product type schema / 2422 属性）并固定回归：深合并相对 legacy 浅合并 `224` 处 shape 升级、`0` 处降级；CHAIR `frame`/`seat` 从 `value` 恢复为 `object`，`maximum_weight_recommendation` 保持 `measure`。8 个 dry_run 新品类 YAML 已重生成，diff 仅含预期 shape 升级与 object 属性 fail-closed 调整。验收：容器内 schema smoke 确认 `frame=object` / `seat=object` / `maximum_weight_recommendation=measure`；CHAIR `meow2511081Gqqd` strict dry-run 未 PUT，按 `blocked_attribute_coverage` fail-closed。验证：targeted tests 22 passed，全量 `.venv/bin/pytest -q` 通过，`git diff --check` 通过。
- ✅ **2026-06-22 / Cursor**: Qwen 真 JSON mode 已生产部署 — 镜像升级为 `amz-listing-management-system:2026-06-22-qwen-json-mode`，主容器 healthy，3 个 scheduler running，`ATTRIBUTE_LLM_EXTRACTION_ENABLED=true` 保持开启。`QwenAPIClient.generate(json_mode=True)` 现透传 DashScope `response_format={"type":"json_object"}`，并在 prompt 未含 JSON 指令时自动补 JSON object 系统提示；保留 JSONDecodeError 截断诊断日志。生产验收：`list-categories` 正常；真实 Qwen json_object smoke 返回 `{'ok': True}`；`meow2511081Gqqd` / `included_components` 连续 5 次真实提取 0 JSON 解析失败；部署后 5 分钟主容器日志无 `JSON解析失败` / `无效JSON响应`。
- ✅ **2026-06-22 / Cursor**: LLM JSON 解析失败诊断日志已生产部署 — 镜像升级为 `amz-listing-management-system:2026-06-22-llm-json-diagnostics`，主容器 healthy，3 个 scheduler running，`ATTRIBUTE_LLM_EXTRACTION_ENABLED=true` 保持开启。`QwenAPIClient` / `DeepSeekAPIClient` 在 `JSONDecodeError` 时记录 provider、model、content_len 与 1000 字符截断 `content_preview`，便于后续直接判断 LLM 返回 malformed JSON 的实际结构。生产验收：`list-categories` 正常，默认 `LLMAttributeExtractor` smoke 成功返回 `included_components`；容器内模拟 Qwen invalid JSON 触发日志，确认输出 `provider=qwen model=qwen-plus-latest content_len=9 content_preview='{bad-json'`。
- ✅ **2026-06-22 / Cursor**: Safe Default Fallback 已生产部署 — 镜像升级为 `amz-listing-management-system:2026-06-22-safe-default-fallback`，主容器 healthy，`amz-price-inventory-scheduler` / `amz-order-sync-scheduler` / `amz-order-daily-report-scheduler` 均 running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302。新增 `amazon_required_safe_defaults_v1`，将 required 属性 default 放行收敛到显式 `safe_default: true` 白名单；`AttributeRuleGenerator` 优先使用 coverage required（schema + learned required），对 eligible required 生成 `path -> llm -> default` 链路，非白名单 default 保持 `null/low` 并继续 fail-closed；`AttributeResolver` 传递 `safe_default` 元数据，`AmazonListingAttributeCoverageGate` 新增 `UNSAFE_DEFAULT_REQUIRED_ATTRIBUTE` 阻断未白名单 default。生产验收：`list-categories` / `pending-statistics` 正常，8 个新品类保持 dry_run 且显示 `needs_rule_review`，CHAIR 仅放行 `number_of_items` safe default，剩余 8 个 blocking 均为真实非白名单缺口；CABINET/HOME_MIRROR strict preview 0 issues。验证：targeted tests 41 passed，全量 `.venv/bin/pytest -q` 与 `git diff --check` 通过。
- ✅ **2026-06-22 / Cursor**: Unified Attribute Pipeline 已生产部署 — 镜像升级为 `amz-listing-management-system:2026-06-22-unified-attribute-pipeline`，主容器 healthy，`amz-price-inventory-scheduler` / `amz-order-sync-scheduler` / `amz-order-daily-report-scheduler` 均 running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302。新增 universal preset `amazon_universal_required_v1`，`AttributeRuleLoader` 支持 preset 合并且 product YAML 覆盖 preset；`AttributeResolver`/YAML/preset 成为商品描述、业务默认与合规默认的唯一属性取值源，`AmazonListingPayloadBuilder` 收敛为结构组装器并改为 YAML 渲染结果覆盖同名属性。8 个新品类 YAML 已重新生成且保持 `mode: dry_run`；生产 `list-categories` / `pending-statistics` 正常，CHAIR strict dry-run 被真实品类缺口阻断且 universal required 已覆盖，CABINET/HOME_MIRROR strict preview 0 issues。验证：targeted tests 42 passed，全量 `.venv/bin/pytest -q` 与 `git diff --check` 通过。
- ✅ **2026-06-22 / Cursor**: VariationHierarchyAuditGate 已生产部署 — 镜像升级为 `amz-listing-management-system:2026-06-22-variation-audit`，主容器 healthy，`amz-price-inventory-scheduler` / `amz-order-sync-scheduler` / `amz-order-daily-report-scheduler` 均 running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302。上线 append child 线上只读变体层级审计、`probe-variation-hierarchy` CLI 与 `variation_resolution_runs` 审计回写；生产 `list-categories` / `pending-statistics` 正常。真实只读 probe：`PARENT-43F84078CC21` 返回 parent ASIN `B0G3L48HF5`、child ASIN `B0G3L9QMM3`，Amazon 未返回可映射变体属性事实，状态 `insufficient_online_facts`，按设计 warning 不阻断。
- ✅ **2026-06-15 / Cursor**: TASK-143 自动类目映射已落地并部署生产 — 新增 `AutoCategoryMapper` 与 CLI `auto-discover-category`，支持 `--category-code` / `--all-unmapped`，默认 dry-run；流程为 Giga 类目采样 listing-eligible 商品 → Catalog Items 搜 ASIN → `includedData=summaries,salesRanks,productTypes` 批量取 ASIN product type → 投票 → 高置信才写入仍为空的 `supplier_categories_map` 并预缓存 schema。修正 `AmazonCatalogClient`：ASIN identifiers 批量 lookup 改为逗号串，product type 从真实 `productTypes[].productType` 提取。生产镜像 `amz-listing-management-system:2026-06-15-auto-category-mapping` 已部署，主容器 healthy，3 个 scheduler running，公网 SSO 302；验收：`10027 / Sofas` dry-run 5/5 投票选 `SOFA`；小批量 no-dry-run 已写入 `10027 -> SOFA` 且 `SOFA` schema cached；部署后新镜像内 dry-run 仍为 `dry_run_selected`。全量 `.venv/bin/pytest -q` 与 `git diff --check` 通过；报告见 `docs/test-reports/2026-06-15-auto-category-mapping.md`。
- ✅ **2026-06-15 / Cursor**: EPIC-AMZ-SCHEMA-DRIVEN-ATTRIBUTES / TASK-142 已生产部署完成 — 镜像升级为 `amz-listing-management-system:2026-06-15-schema-driven-attributes`，主容器 healthy，`amz-price-inventory-scheduler` / `amz-order-sync-scheduler` / `amz-order-daily-report-scheduler` 均 running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302，`list-categories` 返回 CABINET/HOME_MIRROR。HOME_MIRROR strict dry-run：6 个 plan 全部 `validation_preview_passed`、issues=0、无 PUT；12 个已存在 SKU `skipped_existing_scope`，7 个 variation family 仍 `blocked_variation_resolution`（另行处理）。OTTOMAN strict smoke：parent `validation_preview_passed`、2 child `skipped_existing`。CABINET 18-SKU 回归：无 `blocked_attribute_coverage`，1 个既有 `blocked_variation_resolution`，14 个 `skipped_existing_scope`，4 个 width>42in Amazon warning 属 TASK-134 business/live blocker。全量 `.venv/bin/pytest -q` 与 `git diff --check` 通过；报告见 `docs/test-reports/2026-06-15-schema-driven-attributes-production-deploy.md`；`/data/README.md` 与 `/data/TODO.md` 已同步。
- ✅ **2026-06-15 / Cursor**: TASK-141 PayloadBuilder 瘦身与品类硬编码迁移已完成 — CABINET 的 `dimension_strategy: item_depth_width_height` 与 `post_processors: [cabinet_attribute_shapes]` 已迁入 `config/amz_listing_data_mapping/api_attribute_rules/cabinet.yaml`；`AmazonListingPayloadBuilder` 不再保留直接 CABINET 分支，也不再直接 import/call `normalize_cabinet_attributes`，尺寸 shape 和 CABINET schema-shape normalizer 均由配置驱动，通过新增 `attribute_post_processors.py` 统一分发。验证：TASK-141 目标测试 23 passed，发品回归 51 passed，schema pipeline 回归 21 passed，全量 `.venv/bin/pytest -q` 通过，`git diff --check` 通过；报告见 `docs/test-reports/2026-06-15-schema-driven-task-141.md`。
- ✅ **2026-06-15 / Cursor**: TASK-140 LLM 受限属性提取接入 resolver 已完成 — 新增 `LLMAttributeExtractor` / `LLMAttributeExtraction`，`AttributeResolver` 支持 YAML `llm` source，默认不外呼，需注入 extractor/client 后才会执行；受限契约包括缺证据返回 null、`enum_locked` 按 schema valid values 校验、敏感字段（brand/identifier/GTIN/compliance/certification/supplier_declared 等）预拒绝、置信度上限 `medium`。为降低编造风险，required 属性即使由 LLM 提取到也会标记为 `needs_manual_review` 且 blocking，不会静默进入 LIVE。验证：TASK-140 目标测试 10 passed，schema attribute pipeline 组合测试 41 passed，发品回归 44 passed，全量 `.venv/bin/pytest -q` 通过，`git diff --check` 通过；报告见 `docs/test-reports/2026-06-15-schema-driven-task-140.md`。
- ✅ **2026-06-15 / Cursor**: TASK-139 Renderer schema 白名单过滤 + shape 渲染已完成 — `AmazonSchemaService` 新增 `get_property_names()` 暴露 product type schema property allowlist；`AttributePayloadRenderer` 支持 `allowed_attributes`、`measure`、`object`/`nested_object` shape 以及通用 `filter_allowed_attributes()`；`AmazonListingPayloadBuilder` 在返回 plan 前对全量 attributes 执行 schema allowlist 过滤，fail-open 保留离线 dry-run，可通用阻断 HOME_MIRROR 的 `item_depth` / `item_type_name` / `item_width` / `item_height` / `target_audience_base` 这类不适用属性外漏。验证：目标测试 30 passed，发品质量回归 44 passed，全量 `.venv/bin/pytest -q` 通过，`git diff --check` 通过；报告见 `docs/test-reports/2026-06-15-schema-driven-task-139.md`。
- ✅ **2026-06-15 / Cursor**: TASK-138 Schema 深层必填解析 + Coverage Gate 升级已完成 — `AmazonSchemaService` 新增 `get_expanded_required_properties()`、`get_learned_required_properties()`、`get_coverage_required_properties()`，覆盖顶层 required、schema nested / `allOf` / conditional required，并从现有 `amazon_api_submissions.response_body->issues` 学习 Amazon `90220` 缺失必填反馈；`AmazonListingAttributeCoverageGate` 优先使用 coverage required，向后兼容旧 `get_required_properties()`。本阶段无新增数据库 migration。验证：目标测试 23 passed，发品服务回归 37 passed，全量 `.venv/bin/pytest -q` 通过；报告见 `docs/test-reports/2026-06-15-schema-driven-task-138.md`。
- 📋 **2026-06-15 / Cursor**: 新建 Epic `EPIC-AMZ-SCHEMA-DRIVEN-ATTRIBUTES`（Schema-driven Attribute Resolution Pipeline）并落文档 — 针对 HOME_MIRROR 复测仍有 10 缺失必填 + 5 不适用属性的结构性根因（builder 硬编码基础属性+黑名单删除、`get_required_properties` 只取顶层 required、LLM 提取层未接入），确定改造方向为「Schema 白名单驱动 + 证据驱动取值 + LLM 受限提取（只提取不发明）+ preview 反馈学习必填」。已新增 `docs/epics/schema-driven-attribute-resolution.md` 与 ADR `docs/decisions/ADR-2026-06-15-schema-driven-attribute-pipeline.md`，拆分 TASK-138~142（P1 拦得准 / P2 不发非法 / P3 补得全 / P4 瘦身 / 部署验收），待开发推进。
- ✅ **2026-06-15 / Cursor**: TASK-137 Required Attribute Coverage Gate 已落地并部署生产 — 新增 `AmazonListingAttributeCoverageGate`，在 API-native plan 构建后、submitter 前对比 Amazon schema `required_properties` 与 rendered payload；缺失或低置信 required 属性返回 `blocked_attribute_coverage`，不会继续 PUT/VALIDATION_PREVIEW。`fabric_type` 从 `AmazonListingPayloadBuilder` 专用旁路收敛到 CABINET/HOME_MIRROR/OTTOMAN YAML 规则，HOME_MIRROR fallback 为 `Glass, Metal` 且带 evidence。生产镜像 `amz-listing-management-system:2026-06-15-required-coverage`；主容器 healthy，3 个 scheduler running，公网 SSO 302，`list-categories` 正常；HOME_MIRROR strict dry-run 6 个 plan 仍有 Amazon preview issues=15/plan，但 `missing_required=0` 且无 `fabric_type` 相关 issue。验证：相关发品服务测试 45 passed，全量 `.venv/bin/pytest -q` 通过，`git diff --check` 通过；验收报告见 `docs/test-reports/2026-06-15-required-attribute-coverage-gate.md`。
- ✅ **2026-06-15 / Codex**: API-native 发品质量 Epic 已部署到生产 — 生产 compose 镜像升级为 `amz-listing-management-system:2026-06-15`，主服务与 `amz-price-inventory-scheduler` / `amz-order-sync-scheduler` / `amz-order-daily-report-scheduler` 均已重建启动。Smoke：主容器 healthy；公网 `https://amz-listing.meowy.fans` 返回 SSO 302；`list-categories` 返回 CABINET/HOME_MIRROR；legacy `generate-listing --category CABINET` 转发 API-native dry-run 且无 PUT；OTTOMAN 双 SKU strict dry-run 结果为 1 parent `validation_preview_passed`、2 child `skipped_existing`、issues=0、无 PUT。价格/库存 scheduler 首轮完成：273 SKUs processed、269 no-change、4 blocked listing issue、failed 0。部署报告：`docs/test-reports/2026-06-15-api-native-quality-production-deploy.md`；运维登记已同步 `/data/README.md` 与 `/data/TODO.md`。
- ✅ **2026-06-15 / Codex**: TASK-134 CABINET 48in vanity LIVE 策略已决策并落地 — 新增 ADR `docs/decisions/ADR-2026-06-15-cabinet-48in-live-policy.md`。48in vanity 仍允许走 CABINET strict dry-run / Amazon `VALIDATION_PREVIEW` 收集权威 warning，但 `config/listing_gates/quality_gate.yaml` 将 CABINET `item_depth_width_height.width > 42in` 的 `ISSUE_DERIVED_DIMENSION_RANGE` 标记为 `live_blocking: true`；`AmazonListingSubmitter` 在 LIVE 模式将该类 finding 提升为 `blocked_quality_gate`，不调用 PUT。duplicate variation signatures 继续由 resolver fail-closed。目标测试 23 passed。
- ✅ **2026-06-15 / Codex**: TASK-131 Excel legacy retirement 收口已落地 — 新增 `docs/retirement/excel-listing-retirement-2026-06-15.md`，明确 Excel 新品发品、Amazon Excel 模板解析/管理/纠错、template variation config、Excel row variation builder 的退役范围、替代模块和 2026-07-31 删除窗口。相关 legacy 文件与 `generate-listing`/Excel 函数已加脚本可识别 `@retire` 标记；`python3 ~/scripts/check_retirement.py` 识别 15 项待退役，废弃代码约 3.7%。目标 legacy 测试 81 passed。
- ✅ **2026-06-14 / Codex**: TASK-132 CABINET strict validation P0 属性修复已落地并通过 Amazon preview 复验 — 修复 CABINET `door` schema shape、`variation_theme` valid value fallback、`size_name`→`item_width` variation attribute、CABINET 不适用属性移除，以及 Giga comboInfo 尺寸/重量汇总。复跑 CABINET 18-SKU strict dry-run 后，Amazon `VALIDATION_PREVIEW` 从 6 个 plan 全部 issues 降为 4 个 plan 全部 passed，`validation_preview_issues=0`，无 PUT。剩余 blocker 已转为本地 gate/业务策略：2 个 dimension range quality block、2 个 duplicate variation block、3 个 quantity cap block。验证：全量 `.venv/bin/pytest -q` 通过，679 tests collected；报告已追加至 `docs/test-reports/2026-06-14-cabinet-18-strict-dry-run.md`。
- ⚠️ **2026-06-14 / Codex**: CABINET 18-SKU strict dry-run 验收已执行，技术链路通过但业务验收未通过 — 使用 `docs/test-reports/cabinet-18-skus-2026-06-14.txt` 在生产容器临时代码目录执行 `generate-listing-api --strict-validation --only-not-on-amazon`，18 个目标 SKU 未扩散到全品类；9 个 SKU 被 Amazon GET scope 判定已存在并跳过，3 个 `blocked_commercial_gate`，2 个 `blocked_variation_resolution`，6 个 plan 进入 Amazon `VALIDATION_PREVIEW` 且全部为 `validation_preview_issues`，无 PUT。主要 P0 残留：CABINET `door` required 缺失、`variation_theme=Color` 非法、dimension/weight unit 缺失、`item_type_name`/`target_audience_base` 不适用。验收报告：`docs/test-reports/2026-06-14-cabinet-18-strict-dry-run.md`。
- ✅ **2026-06-14 / Codex**: EPIC Phase 4 SKU 级发品范围控制与审计摘要已落地 — `generate-listing-api` 支持 `--sku`（可重复/逗号分隔）、`--sku-file`、`--only-not-on-amazon`，CLI 参数贯通到 `ProductListingService` 和 API-native plan builder。新增 `ListingScope` / `ProductListingScopeFilter`，显式 SKU 会先按本地 eligible + category 映射过滤，非 eligible / 类目不匹配进入 `blocked_scope_filter` 审计结果；`--only-not-on-amazon` 只读调用 `getListingsItem`，已存在 SKU 返回 `skipped_existing_scope`，非 404 错误 fail closed。service 返回 `audit.scope` 与 `audit.result_status_counts`，CLI 打印状态摘要，可用于精确复跑 CABINET 18-SKU 验收集。验证：scope/service/CLI 目标测试 46 passed；全量 `.venv/bin/pytest -q` 通过，676 tests collected。
- ✅ **2026-06-13 / Codex**: EPIC Phase 3 strict dry-run + Amazon `VALIDATION_PREVIEW` 预检闭环已落地 — `main.py` 新增 `--strict-validation` 与 `LISTING_STRICT_VALIDATION=true` 显式开关；`generate-listing-api` 在 strict dry-run 下执行实时 `getListingsItem` 查重和 `VALIDATION_PREVIEW`，但不会调用 PUT；预检结果写入 `amazon_api_submissions`，状态区分 `validation_preview_passed` / `validation_preview_issues` / `validation_preview_failed` / `skipped_existing`。`--strict-validation --no-dry-run` 会被 CLI 阻断。验证：submitter/CLI/product listing 目标测试 53 passed；全量 `.venv/bin/pytest -q` 通过，668 tests collected。
- ✅ **2026-06-13 / Codex**: EPIC Phase 2 Attribute Resolver v1 已落地 — 新增 `AttributeRuleLoader`、`AttributeResolver`、`AttributePayloadRenderer` 与 `config/amz_listing_data_mapping/api_attribute_rules/cabinet.yaml` / `ottoman.yaml`；`AmazonListingPayloadBuilder` 接入 resolver 作为配置驱动补充层，CABINET 的 mounting/model/construction/number/items/door/included/is_assembly/item_shape/room_type 等属性不再继续通过 builder hardcode 扩展。验证：resolver + payload builder 目标测试 6 passed；全量 `.venv/bin/pytest -q` 通过，661 tests collected。
- ✅ **2026-06-13 / Codex**: EPIC Phase 1 LLM 内容生成双阶段 Review Gate 已落地 — 新增 `ProductContentReviewer`、`ContentReviewResult` 和 `product_content_review` prompt；`ProductContentGenerator` 在生成后执行 deterministic scanner + Reviewer LLM，`pass` 才进入保存/后续发品，`revise` 最多自动重生成 1 次，`reject`/`manual_review` 阻断；`ProductDetailGenerationService` 将 review status/attempt/result 写入 `ds_api_product_details.raw_json`。验证：目标内容测试 27 passed；全量 `.venv/bin/pytest -q` 通过，657 tests collected。
- ✅ **2026-06-13 / Codex**: 确认发品架构方向调整为 API-native only：`generate-listing` / Excel 新品发品流程 deprecated，交互菜单与 README 切到 `generate-listing-api` 作为唯一新品发品入口；Excel 模板解析/纠错入口从交互菜单移除，类目接入主路径切为 Product Type Discovery + 映射建议；新增 `docs/module-design/api-attribute-resolution.md` 设计草案，要求后续多类目属性解析走 schema-driven + configurable resolver，避免在 payload builder 中继续堆类目 hardcode。CLI 目标测试与全量 pytest 已通过。
- ✅ **2026-06-11 / Cursor**: Amazon 订单同步 Phase A 上线 — 新增 `AmazonOrdersClient`、`AmazonOrderSyncService`、`AmazonOrderDailyReportService`、`amazon_orders` 表（Alembic `009`）；策略：30min 轮询、无变更不飞书、同步失败 P1/P0 告警、北京时间 9:00 发送 24h 健康日报；生产 compose 新增 `amz-order-sync-scheduler` 与 `amz-order-daily-report-scheduler`，镜像 `2026-06-09`，4 容器 running；订单相关单测 9 项通过；已同步 `/data/README.md` 与 `/data/TODO.md` 服务登记。
- ✅ **2026-06-08 / Codex**: 价格/库存更新增加 30 分钟二次确认 — 新增 `AmazonPriceInventoryDelayedConfirmationService` 和 CLI `confirm-price-inventory-api`，扫描 30 分钟前 `confirmed_with_mismatch` / `confirmed_with_issues` / `accepted_pending_confirmation` / `confirmation_failed` 且尚未二次确认的提交，只调用 `getListingsItem`，写入新的 `amazon_api_submissions` 行（`operation=delayed_confirmation`，`response_body.source_submission_id` 指回原提交），不重复 PATCH。scheduler 每轮先跑二次确认，再跑新的价格/库存更新。新增状态：`delayed_update_confirmed`、`delayed_confirmed_with_mismatch`、`delayed_confirmed_with_issues`、`delayed_confirmed_with_issues_and_mismatch`、`delayed_confirmation_failed`。生产验证：手工执行二次确认处理 480 条历史记录，其中 `delayed_update_confirmed=230`；`submission_id=1123` 已确认为 `delayed_update_confirmed` 且 mismatch 清空。全量 `pytest -q` 通过（608 tests）。
- ✅ **2026-06-08 / Codex**: 价格/库存每小时更新已部署到生产 — 生产镜像升级为 `amz-listing-management-system:2026-06-08`，`amz-price-inventory-scheduler` 已启动并每小时执行 `update-price-inventory-api --no-dry-run`。生产部署时补两项真实环境修复：`searchListingsItems` 翻页时保持原搜索参数，避免 Amazon `pageToken` 参数不匹配；新增 Alembic `008_giga_base_price_sku_unique`，清理 `giga_product_base_prices` 重复 SKU 并补 `giga_sku` 唯一索引，修复 Giga 价格同步 `ON CONFLICT(giga_sku)` 失败。生产验证：主容器 healthy，scheduler running，Alembic `008_giga_base_price_sku_unique (head)`；手工执行真实 `update-price-inventory-api --no-dry-run` 完成 `273 SKUs processed`，`Failed: 0`。最近 273 条提交状态：`skipped_no_change=16`、`blocked_listing_issue=11`、`confirmed_with_issues=127`、`confirmed_with_mismatch=119`。
- ✅ **2026-06-08 / Codex**: 价格/库存 API 更新增加每小时生产调度 — 生产 compose 新增 `amz-price-inventory-scheduler` 服务，使用同一镜像每小时执行 `python main.py --task update-price-inventory-api --no-dry-run`，默认首次启动延迟 120 秒，间隔由 `PRICE_INVENTORY_UPDATE_INTERVAL_SECONDS` 控制。新增 `scripts/hourly_price_inventory_update.sh` 和 `PostgresAdvisoryLock`，`update-price-inventory-api` 执行前获取 PostgreSQL advisory lock，避免定时任务与手工任务重叠；任务异常会重新抛出，使 CLI/调度日志能看到失败。验证：目标 27 tests passed；脚本 `sh -n` 通过；临时生产 compose config 校验通过；全量 `pytest -q` 通过（600 tests）。
- ✅ **2026-06-08 / Codex**: 价格/库存 API 更新切到 API-native — 新增 `amazon_listing_items_cache` 表与 Alembic `007_amazon_listing_items_cache`，新增 `AmazonListingItemCacheRepository` 和 `AmazonPriceInventoryUpdateService`。`update-price-inventory-api` 不再从 `amz_all_listing_report` 取 Active SKU，而是先通过 `searchListingsItems` 刷新 API cache，再从 cache + `meow_sku_map` 生成候选；每个 SKU PATCH 前执行 `getListingsItem` 实时确认、ERROR issue 阻断、no-change diff，只对变化字段调用 `patchListingsItem`；PATCH 后再次 `getListingsItem` 对比目标价格/库存，区分 `update_confirmed`、`confirmed_with_mismatch`、`confirmed_with_issues`、`accepted_pending_confirmation`、`confirmation_failed` 等状态。`dry_run` 仍不写 Amazon，但会执行 API 读同步。验证：相关 19 tests passed；`alembic upgrade head --sql` 通过；全量 `pytest -q` 通过（594 tests）。
- ✅ **2026-06-08 / Codex**: 发品提交后确认闭环落地 — `AmazonListingSubmitter` 在真实 `putListingsItem` 返回 `ACCEPTED` 且 `issues=[]` 后，会立即执行一次 `getListingsItem` 确认，不再只把 PUT accepted 当作最终成功。新增状态：`listing_confirmed`（GET 可查且无 issues）、`confirmed_with_issues`（GET 可查但有 issues）、`accepted_pending_confirmation`（PUT accepted 但 GET 仍 404）、`confirmation_failed`（PUT accepted 但确认 GET 异常）、`not_accepted`（PUT 返回非 ACCEPTED 且无 issues）。确认失败不会重写 PUT 结果为发品失败，提交记录会保存 `put_response` + `confirm_response` 或确认错误。`dry_run` 和 `validation_only` 不做提交后确认。验证：submitter 12 tests passed；相关 29 tests passed；全量 `pytest -q` 通过（592 tests）。
- ✅ **2026-06-08 / Codex**: API 发品变体解析与增量子体支持落地 — 新增 `AmazonVariationResolver`、`ProductListingAPIPlanBuilder`、`amazon_variation_resolution_runs` 表与 Alembic `006_variation_resolution_runs`，`generate-listing-api` 在构建 payload 前以 deterministic-first 策略选择 variation theme，并将候选属性、评分、历史家族和阻断原因落审计。已支持 Giga 新增 SKUE 到已发布 SKUA-D 父体的场景：通过 `associateProductList` 映射历史 meowy SKU，继承既有 `parent_sku` / `variation_theme`，只生成新 child payload；若 child 属性组合重复或缺失，返回 `blocked_variation_resolution`。同时将 API plan 构建从 `ProductListingService` 拆到独立 builder，原 service 从 685 行降至 343 行。验证：新增/相关 28 tests passed；`alembic upgrade head --sql` 通过；全量 `pytest -q` 通过（587 tests）。
- ✅ **2026-06-08 / Codex**: 价格/库存 Commercial Gate 可审计守门落地 — 新增 `config/listing_gates/commercial_gate.yaml`、`amazon_listing_commercial_gate_runs` 表与 Alembic `005_commercial_gate_runs`，新增 `AmazonListingCommercialGate` 和审计仓库。`ProductDataRepository` 扩展价格/库存审计字段，发品库存口径从 `quantity + buyer_qty` 调整为明确的 `giga_inventory.quantity`，`buyer_qty/seller_qty` 仅进入审计快照。`generate-listing-api` 构建 plan 前先执行 Commercial Gate，passed/blocked 都落审计表；blocked SKU 返回 `blocked_commercial_gate`，不进入 payload/submitter。规则覆盖价格缺失/无效、currency、价格新鲜度、最低毛利、库存新鲜度、负库存、零库存策略、发布库存上限。验证：新增/相关 22 tests passed；`alembic upgrade head --sql` 通过；全量 `pytest -q` 通过（577 tests）。
- ✅ **2026-06-08 / Codex**: 图片资产框架第一版落地 — 新增 `product_image_assets` 表与 Alembic `004_product_image_assets`，建立 raw / auto_approved / needs_review / approved / rejected / processing_failed 状态模型；新增 `AmazonListingImageAssetRepository`、`AmazonListingImageInspectionService`、`AmazonListingImageAssetService`、`AmazonListingImageSelector`。第一版实现 HTTPS、HEAD 可访问性、Content-Type、Content-Length、supplier-hosted 风险标记等确定性检查；预留白底/OCR/水印/主体占比/抠图/对象存储/人工审核 UI 扩展点。`generate-listing-api` 发品时只读取 approved/auto_approved 图片资产覆盖 raw 图片，不在发品过程中联网探测。验证：新增/相关 17 tests passed；`alembic upgrade head --sql` 通过；全量 `pytest -q` 通过（569 tests）。
- ✅ **2026-06-08 / Codex**: API 发品主路径切到 API-native draft/payload — 新增 `AmazonListingDraft`、`AmazonListingDraftBuilder`、`AmazonListingPayloadBuilder`，`generate-listing-api` 不再生成 Excel-like 中间行，也不再依赖 `amazon_cat_templates` 模板规则；API 路径直接从 `ProductDataRepository` 数据构建 `AmazonListingDraft`，再渲染为 SP-API attributes plan。Excel 发品路径继续保留旧模板链路。Payload builder 优先使用 cached Product Type Definitions schema 的 valid values 做枚举对齐，避免 dry-run 意外外呼；submitter 继续执行质量闸门、实时查重、VALIDATION_PREVIEW/putListingsItem。验证：全量 `pytest -q` 通过（562 tests）。
- ✅ **2026-06-08 / Codex**: API 发品查重切换为 Listings Items API 实时事实源 — `ProductListingRepository.get_pending_listing_skus()` 不再通过 `amz_all_listing_report` 排除 Amazon 已存在 SKU，只保留 Giga 本地发布资格筛选；`AmazonListingSubmitter` 在非 dry-run 提交前调用 `getListingsItem` 实时查重，404 才允许 `VALIDATION_PREVIEW` / `putListingsItem`，200 记录 `skipped_existing` 并跳过，查重异常记录 `failed_existing_check` 且不提交。验证：全量 `pytest -q` 通过（555 tests）。
- ✅ **2026-06-08 / Codex**: 内容生成主链路切到 v2 — `generate-details` 现在使用 `GigaProductNormalizer` 将 Giga raw_data 标准化为 `StandardProduct`，再调用 `ProductContentGenerator` / `prod_detail_gen_v2` 生成品类感知标题、5 点、描述、search_terms 与 generic_keyword；现有 `ds_api_product_details` 表契约保持不变，`raw_json` 记录 v2 元数据、product_type、backend keywords、enriched_attributes 与 validation_warnings。新增 `LLMProductDetailRepository.get_product_type_for_sku()` 从 `supplier_categories_map` 解析 Amazon 标准品类。验证：相关 30 tests passed；全量 `pytest -q` 通过（553 passed）。
- ✅ **2026-05-23 / Codex**: Phase 3 实施完成 — 新增 `AmazonAdsClient`（Ads API 广告 Campaign 管理+报告）、`AmazonBrandAnalyticsClient`（BA SQP 搜索词份额）、`PPCManagementService`（新 Listing 自动广告创建+关键词收割+否定词管理+预算调度）、`ProfitAnalyzer`（单品利润核算+损益拆解）、`InventoryPlanner`（库存健康分析+补货推荐+清货策略）、`ContentPerformanceAnalyzer`（A+ 效果分析+版本 CVR 对比）、`ProductLifecycleManager`（SELECTED→PREPARING→LAUNCHED→GROWING→MATURE→DECLINING 完整状态机+每阶段任务清单+自动流转规则）。新增 SQL 迁移 `create_phase3_tables.sql`（7 张表：product_lifecycle_states, ad_performance_snapshot, product_profit_snapshot, inventory_health_log, daily_check_log, weekly_report_snapshot, notification_log）。CLI 新增 3 个任务：`profit-analysis`, `inventory-health`, `lifecycle-summary`。Dispatcher 总任务数 31。`pytest -q` 全部通过。
- ✅ **2026-05-23 / Codex**: Phase 2 实施完成 — 新增 `AmazonPricingClient`（Product Pricing API v0：getCompetitivePricing / getItemOffers / getItemOffersBatch）、`AmazonCatalogClient`（Catalog Items API v2022-04-01：getCatalogItem / searchCatalogItems / BSR 查询）、`CompetitiveIntelService`（竞品价格+BSR+卖家密度 → 竞争力 S/A/B/C 评分 → 调价建议）、`KeywordRankingTracker`（关键词排名追踪+变化检测+快照持久化）、`ReviewSentimentAnalyzer`（LLM 评论痛点提取+Listing 改进建议）、`WeeklyReportService`（关键词排名+竞品动态+Listing 健康+广告表现聚合周报）。新增 SQL 迁移 `create_competitive_tables.sql`（4 张表：competitor_asin_registry, competitive_pricing_snapshot, keyword_ranking_tracking, keyword_ranking_log）。CLI 新增 2 个任务：`competitive-analysis`, `weekly-report`。`pytest -q` 全部通过。
- ✅ **2026-05-23 / Codex**: Phase 1 实施完成 — 新增 `FeishuClient`（P0/P1/P2 分级飞书推送）、`KeywordResearchService`（LLM 扩词 50+ → 分层分配标题/5点/COSMO 属性）、`DailyCheckService`（Listing 问题+库存预警+飞书日报）、`deepseek.yaml` 新增 `keyword_expansion` 和 `keyword_allocation` 两个 Prompt。增强 `ProductContentGenerator` 接受 `KeywordResearchResult` 注入、`AmazonListingQualityGate` 新增 COSMO 后台属性完整性检查。CLI 新增 2 个任务：`keyword-research`, `daily-check`。`pytest -q` 全部通过。
- ✅ **2026-05-23 / Codex**: 真实数据源改造 — 重写 5 个 Phase 2-3 Handler，全部从 Demo 硬编码改为真实数据库查询。新增 7 个数据解析 Helper：`_resolve_sku_to_asin`（amz_all_listing_report）、`_resolve_meow_to_giga`（meow_sku_map）、`_resolve_giga_to_name`（giga_product_sync_records）、`_resolve_giga_to_cost`（giga_product_base_prices）、`_resolve_meow_to_price`（amz_all_listing_report）、`_resolve_giga_to_inventory`（giga_inventory）、`_get_active_listings`。`competitive-analysis` 现在使用真实 ASIN + 真实成本价；`profit-analysis` 使用真实售价 + 真实 landed_cost；`inventory-health` 使用真实库存数量 + 产品名称；`lifecycle-summary` 使用 `meow_sku_map` + `amz_listing_log` 推断生命周期阶段。`pytest -q` 全部通过。
- ✅ **2026-05-18 / Codex**: OTTOMAN Storage Bench 全链路端到端验收通过：Giga Sync → SKU Mapping → Product Type Discovery (OTTOMAN) → Schema Cache → LLM Content Generation → Attribute Mapping → Variation (1 parent + 2 children, COLOR theme) → VALIDATION_PREVIEW → putListingsItem。5/5 ACCEPTED，32/32 fields 数据一致，价格 $123.34 经定价公式验证，库存 0/40 与 Giga 源数据一致。验收文档 `docs/acceptance/ottoman-e2e-2026-05-18.md`。`pytest -q` 512 passed。
- ✅ **2026-05-18 / Codex**: 新增 `sp_api_ottoman.json` 品类覆盖配置（frame.material / seat / top / base 嵌套对象、variation_theme.name 格式），`AmazonAttributeMapper` 新增 `ottoman_dim_*`、`variation_theme_ottoman` 类型。发现 OTTOMAN 使用 `frame[].material` 而非 `frame_material`、selector 字段 `language_tag` 等品类特定差异。
- ✅ **2026-05-18 / Codex**: Phase A-E 多源标准化发品改造：`StandardProduct` + `GigaProductNormalizer`、`AmazonSchemaService`（schema 缓存/校验/合法值）、`ProductContentGenerator`（品类感知 LLM + 后验证）、`discover-product-type` / `suggest-category-mappings` CLI、变体 API 映射。23 个 CLI 任务。`pytest -q` 512 passed。
- ✅ **2026-05-18 / Codex**: 开发规范偏移修复：红线级抽取 `AmazonProductTypeSchemaRepository`、拆分 `_convert()`（104→5 个 group handler）、拆分 `product_content_generator.py`（320→284 行）+ `content_validator.py` 提取、`amazon_attribute_mapper.py`（318→298 行）、修复死 logger/export/类型注解。
- ✅ **2026-05-17 / Codex**: 完成 Phase 4 新品发品 API 出口 + Phase 3 价格库存更新 + Phase 2 Reports API + Phase 1 基础设施。所有 SP-API 操作经上海阿里云 ECS 堡垒机固定出口。
- ✅ **2026-05-04 / Codex**: 修复库存同步单测挂起问题，完整测试从卡住恢复为 `52 passed in 1.34s`。
- ✅ **2026-05-04 / Codex**: 调整 `.gitignore`，不再屏蔽 `tests/` 下的正式 `test_*.py` 单测文件。
- ✅ **2026-05-04 / Codex**: 建立覆盖率基线：当前 `127 passed in 9.12s`，总覆盖率 `60.60%`。
- ✅ **2026-05-04 / Codex**: 已新增 `.github/workflows/ci.yml` 并接入 repo-level runner `amz-listing-runner-01`；CI run `25330003050` 在 commit `c5dc219` 绿色通过。
- ✅ **2026-05-04 / Codex**: 补齐关键模块设计文档和 service 返回契约草案，覆盖发品、Giga同步、价格库存、模板映射。
- ⏳ **2026-05-04 / Codex**: 开始拆分 `main.py` 前置收敛，已将重复的非交互任务分发逻辑合并到 `_dispatch_task()`；`pytest` 仍为 52 passed。
- ⏳ **2026-05-04 / Codex**: 修复无 `--task` 时交互菜单不可达的问题，并将交互菜单壳拆到 `src/cli/menu.py`；`main.py` 从 937 行降至 843 行。
- ⏳ **2026-05-04 / Codex**: 将任务分发改为注册表形式，并为 `src/cli/menu.py` 补 3 个单测；`pytest` 当前 55 passed，覆盖率 39%。
- ⏳ **2026-05-04 / Codex**: 将只读查询 handler 拆到 `src/cli/query_handlers.py`，并补 3 个单测；`main.py` 当前 673 行。
- ⏳ **2026-05-04 / Codex**: 将类目配置 handler 拆到 `src/cli/category_handlers.py`，并补 4 个单测；`main.py` 当前 445 行，低于 500 行红线。
- ⏳ **2026-05-04 / Codex**: 将发品生成 handler 拆到 `src/cli/listing_handlers.py`，并补 2 个单测；`main.py` 当前 378 行。
- ✅ **2026-05-04 / Codex**: 将运营 handler 拆到 `src/cli/operation_handlers.py`，任务注册拆到 `src/cli/task_dispatcher.py`；`main.py` 当前 96 行，已低于 150 行目标。
- ✅ **2026-05-04 / Codex**: 完成 Phase 4 主要输出边界收敛，`PricingService`、`GigaSyncService`、`GigaInventorySyncService`、`GigaPriceSyncService` 均支持可替换 progress reporter，默认 CLI 输出保持不变，并补静默 reporter 单测。
- ✅ **2026-05-04 / Codex**: 继续收敛更新文件编排服务，`InventoryPriceUpdaterService` 支持可替换 progress reporter，并将同一 reporter 传递给价格、库存、售价子服务。
- ✅ **2026-05-04 / Codex**: 继续收敛 AI 商品详情生成服务，`ProductDetailGenerationService` 支持可替换 progress reporter，默认 CLI 输出保持不变，并补无 SKU/分批处理静默单测。
- ✅ **2026-05-04 / Codex**: 继续收敛品类维护服务，`CategoryMaintenanceService` 支持可替换 progress reporter，默认 CLI 输出保持不变，并补同步/CSV 静默单测。
- ✅ **2026-05-04 / Codex**: 将 `PricingService` 的本地 reporter 类收敛为统一 `progress_reporter.py` 的兼容子类，保留原导出类名。
- ✅ **2026-05-04 / Codex**: 继续收敛全量报告导入与发品状态更新服务，`AmzFullListImporterService`、`ListingStatusManager` 支持可替换 progress reporter，并补事务/静默单测。
- ✅ **2026-05-04 / Codex**: 继续收敛品类判定服务，`CategoryService` 支持可替换 progress reporter，`PricingService` 会传递同一 reporter 到品类判定链路。
- ✅ **2026-05-04 / Codex**: 继续收敛模板管理服务，`TemplateManagementService` 支持可替换 progress reporter；`input` 提示改为 reporter 输出后再读取输入，默认交互能力保留。
- ✅ **2026-05-04 / Codex**: 补充核心发品服务分支测试，覆盖品类无 SKU、缺模板、Excel 生成异常回滚、变体父子行与日志生成路径；`ProductListingService` 覆盖率提升至 80%。
- ✅ **2026-05-04 / Codex**: 补充 SKU 映射服务分支测试，覆盖空源、全已映射、创建新映射、插入异常回滚、SKU 生成冲突重试失败路径；`SkuMappingService` 覆盖率提升至 93%。
- ✅ **2026-05-04 / Codex**: 补充变体主题服务 LLM 判定测试，覆盖首轮成功、重复属性二轮纠正、缺 prompt fallback、LLM 异常 fallback、尺寸格式化和 HTML 清洗；`VariationThemeService` 覆盖率提升至 93%。
- ✅ **2026-05-04 / Codex**: 补充数据映射助手核心规则测试，覆盖有效值对齐、字段引用、JSONB fallback、数组/组合计算、尺寸/重量/品类查找、LLM 增强与 fallback；`DataMappingHelper` 覆盖率提升至 77%。
- ✅ **2026-05-04 / Codex**: 拆分 `DataMappingHelper` 文件规模红线，将 LLM 增强逻辑迁移到 `src/utils/data_mapping_llm.py`，原 helper 保留兼容委托方法并降至 445 行。
- ✅ **2026-05-04 / Codex**: 更新架构文档，补齐当前 `src/cli/*` 分层、`ProgressReporter` 输出边界和 `data_mapping_llm` 映射增强模块。
- ✅ **2026-05-04 / Codex**: 拆分 `ProductListingService` 变体构造职责，将父子行和变体日志构造迁移到 `ProductListingVariationBuilder`，原 service 降至 369 行。
- ✅ **2026-05-04 / Codex**: 拆分模板规则矫正辅助逻辑，将 Amazon 报错报告解析和必填规则矫正迁移到 `amz_template_rule_correction.py`，模板管理服务降至 389 行。
- ✅ **2026-05-04 / Codex**: 拆分品类映射 CSV 更新流程，将文件读取、行验证、批量更新和输出统计迁移到 `CategoryMappingCsvUpdater`，品类维护服务降至 227 行。
- ✅ **2026-05-04 / Codex**: 拆分 `DataMappingHelper` 单字段映射职责，将 source type 映射、JSONB 读取、单位换算和重量计算迁移到 `DataFieldMapper`，原 helper 降至 331 行；覆盖率 `55.42%`。
- ✅ **2026-05-04 / Codex**: 拆分模板解析纯规则，将 Data Definitions 表头/字段提取、Valid Values 解析迁移到 `template_parser_helpers.py`，`amz_template_parser.py` 降至 285 行；parser 覆盖率提升至 71%，总覆盖率 `58.40%`。
- ✅ **2026-05-04 / Codex**: 拆分模板变体配置职责，将变体字段映射、优先主题用户输入/历史/default 选择迁移到 `template_variation_config.py`，模板管理服务降至 291 行；总覆盖率 `58.53%`。
- ✅ **2026-05-04 / Codex**: 拆分核心发品服务配置/结果/日志职责，将品类配置加载、待发 SKU 过滤、结果字典和日志 payload 构造迁移到独立 helper，`product_listing_service.py` 降至 294 行；总覆盖率 `58.84%`。
- ✅ **2026-05-04 / Codex**: 拆分变体主题 LLM prompt 准备与属性格式化职责，将产品清洗、prompt 组装、优先主题过滤、唯一性校验和格式化迁移到 `variation_theme_helpers.py`，服务降至 229 行；总覆盖率 `58.93%`。
- ✅ **2026-05-04 / Codex**: 拆分数据映射有效值对齐与 LLM task 提取职责，将有效值对齐、模糊匹配和 LLM 任务提取迁移到 `data_mapping_valid_values.py` / `data_mapping_tasks.py`，`data_mapping_helper.py` 降至 261 行；总覆盖率 `59.33%`。
- ✅ **2026-05-04 / Codex**: 拆分 Giga 价格仓库数据转换职责，将无效价格过滤、按 Giga 指数去重、base/tier row 构造迁移到 `giga_price_transform.py`，`giga_product_price_repository.py` 降至 277 行；总覆盖率 `60.60%`。
- ✅ **2026-05-05 / Codex**: 新增核心发品链路集成测试，覆盖 `ProductListingService` 与变体识别、字段映射、Excel 生成、发品日志构造、事务提交的协作路径；`pytest` 当前 128 passed，总覆盖率 `60.79%`。
- ✅ **2026-05-05 / Codex**: 新增发品 repository SQL 边界集成测试，固定待发品筛选、变体 latest_records 查询、品类映射大小写 join、空输入和异常重抛契约；`pytest` 当前 134 passed，总覆盖率 `61.02%`，`ProductListingRepository` 覆盖率 88%。
- ✅ **2026-05-05 / Codex**: 新增 Giga 商品同步编排集成测试，覆盖 SKU 分页获取、详情批处理、repository 保存、事务提交/回滚、统计和 reporter 输出；`pytest` 当前 137 passed，总覆盖率 `61.28%`，`GigaSyncService` 覆盖率 88%。
- ✅ **2026-05-05 / Codex**: 新增 Giga 价格同步编排集成测试，覆盖价格批处理、API 重试、限流等待、repository 保存、事务提交/回滚和统计输出；`pytest` 当前 140 passed，总覆盖率 `62.74%`，`GigaPriceSyncService` 覆盖率 98%。
- ✅ **2026-05-05 / Codex**: 新增 Giga 库存同步编排集成测试，覆盖库存批次调度、成功/失败批次聚合、限流等待、仓库异常兜底和 reporter 输出；`pytest` 当前 142 passed，总覆盖率 `62.90%`，`GigaInventorySyncService` 覆盖率 87%。
- ✅ **2026-05-05 / Codex**: 新增库存价格更新文件生成集成测试，覆盖 SKU/价格/库存整合、TSV 输出列顺序、空价格/空库存兜底和 reporter 输出；`pytest` 当前 143 passed，总覆盖率 `63.59%`，`InventoryPriceUpdaterService` 覆盖率 96%。
- ✅ **2026-05-05 / Codex**: 新增库存价格更新 repository SQL 边界集成测试，固定可更新 SKU 映射、价格/库存批量查询、空输入短路和异常兜底契约；`pytest` 当前 149 passed，总覆盖率 `64.31%`，`ListingDataRepository` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增 CLI 非交互入口冒烟测试，覆盖 `main.py --task` 参数传递、DB session 生命周期、成功退出码和未知任务退出码；`pytest` 当前 151 passed，总覆盖率 `64.31%`。
- ✅ **2026-05-05 / Codex**: 新增定价 repository SQL/COPY 边界集成测试，固定 SKU 列表、成本查询价格选择、非 USD/无价跳过、COPY upsert 和异常重抛契约；`pytest` 当前 158 passed，总覆盖率 `65.61%`，`PricingRepository` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增 Giga 价格 repository SQL/COPY 边界集成测试，固定 SKU 查询、base/tier COPY 写入、统计查询、批量失败和 tier 失败兜底契约；`pytest` 当前 171 passed，总覆盖率 `67.07%`，`GigaProductPriceRepository` 覆盖率 98%。
- ✅ **2026-05-05 / Codex**: 新增 Giga 库存 repository SQL/COPY 边界集成测试，固定 SKU 查询、库存解析、临时表 COPY upsert、提交/回滚和统计查询契约；`pytest` 当前 181 passed，总覆盖率 `68.29%`，`GigaProductInventoryRepository` 覆盖率 99%。
- ✅ **2026-05-05 / Codex**: 新增 Giga 商品同步 repository SQL 边界集成测试，固定 product upsert、batch upsert、按 SKU 查询、统计和 SKU 列表契约；`pytest` 当前 190 passed，总覆盖率 `69.18%`，`GigaProductSyncRepository` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增 SKU 映射 repository SQL 边界集成测试，固定映射查询、LLM SKU 查询、未映射过滤、批量插入和统计契约；`pytest` 当前 200 passed，总覆盖率 `70.01%`，`SkuMappingRepository` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增 LLM 商品详情 repository SQL 边界集成测试，固定未处理 SKU 查询、raw_data 字典/JSON 读取、批量保存 commit/rollback 和统计契约；`pytest` 当前 210 passed，总覆盖率 `70.95%`，`LLMProductDetailRepository` 覆盖率 98%。
- ✅ **2026-05-05 / Codex**: 新增发品日志 repository SQL 边界集成测试，固定 family 查询、parent 查询、变体属性 JSON 解析、批量日志 upsert 和状态更新契约；`pytest` 当前 220 passed，总覆盖率 `72.19%`，`AmzListingLogRepository` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增模板 repository SQL 边界集成测试，固定模板保存 JSON 序列化、按品类查询 JSON 解析/默认值、字段定义更新不提交事务、优先主题列表查询契约；`pytest` 当前 235 passed，总覆盖率 `73.91%`，`AmzTemplateRepository` 覆盖率 99%。
- ✅ **2026-05-05 / Codex**: 新增品类 repository SQL 边界集成测试，固定 SKU 到标准品类映射、供应商品类发现、未映射统计、有效 Amazon 品类集合、批量插入/更新事务契约；`pytest` 当前 251 passed，总覆盖率 `75.56%`，`CategoryRepository` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增全量报告 repository SQL 边界集成测试，固定 DataFrame 临时表写入、upsert/drop、异常清理和统计查询契约；`pytest` 当前 257 passed，总覆盖率 `76.05%`，`AmzFullListReportRepository` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增产品数据 repository SQL 边界集成测试，固定完整产品数据 join 查询、缺失返回空字典和数据库异常重抛契约；`pytest` 当前 260 passed，总覆盖率 `76.36%`，`ProductDataRepository` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增发品配置加载单元测试，固定默认配置路径发现、显式 category_details 加载、缺失文件和坏 JSON 兜底契约；`pytest` 当前 268 passed，总覆盖率 `76.83%`，`product_listing_config.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增价格配置加载单元测试，固定 YAML 加载缓存、fallback、品类覆盖、缺失文件和坏 YAML 异常契约；`pytest` 当前 274 passed，总覆盖率 `77.30%`，`pricing_config.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增数据清洗工具单元测试，固定递归清洗、HTML/图片清理、智能截断降级和简单截断契约；`pytest` 当前 285 passed，总覆盖率 `78.69%`，`data_cleaner.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 扩展 AI 商品详情生成服务单元测试，固定单 SKU 生成、缺数据/缺 prompt、LLM 重试/最终失败、批处理保存统计契约；`pytest` 当前 292 passed，总覆盖率 `79.72%`，`ProductDetailGenerationService` 覆盖率 97%。
- ✅ **2026-05-05 / Codex**: 新增 Prompt 管理器单元测试，固定 prompt YAML 加载、缺文件/坏 YAML 容错、缺失 prompt 和 reload 契约；`pytest` 当前 298 passed，总覆盖率 `79.98%`，`prompt_manager.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增输入校验和异常单元测试，固定文件路径校验、目录/缺失/格式拒绝和业务异常 code 契约；`pytest` 当前 307 passed，总覆盖率 `80.19%`，`validators.py` 与 `exceptions.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增直接 LLM 服务适配器单元测试，固定 provider 路由、默认模型、fallback、不可用 provider 和健康检查契约；`pytest` 当前 317 passed，总覆盖率 `81.06%`，`DirectLLMService` 覆盖率 94%。
- ✅ **2026-05-05 / Codex**: 新增 LLM API 客户端单元测试，固定 DeepSeek/Qwen 初始化、文本/JSON 解析、空内容、API 错误和异常重抛契约；`pytest` 当前 329 passed，总覆盖率 `81.91%`，DeepSeek/Qwen 客户端覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增 Giga API 客户端单元测试，固定 Giga 配置、token 缓存/刷新、API 成功、401 刷新、业务错误、超时和可重试异常契约；`pytest` 当前 344 passed，总覆盖率 `83.79%`，Giga config/token/API client 覆盖率 100%/90%/93%。
- ✅ **2026-05-05 / Codex**: 新增生产部署模板 `deploy/production/`，按 `/data/docker-compose/amz-listing-management-system/`、共享 PostgreSQL `postgres:5432`、无宿主机端口、`.env` 敏感配置和 `/data/volumes` 持久化路径固化部署契约；compose config 校验通过，`pytest` 当前 344 passed。
- ✅ **2026-05-05 / Codex**: 新增 Autogen LLM 适配器单元测试，固定 payload、session、fallback、健康检查、final message 解析和上游错误契约；`pytest` 当前 356 passed，总覆盖率 `85.84%`，`AutoGenLLMService` 覆盖率 98%。
- ✅ **2026-05-05 / Codex**: 新增运营 CLI handler 单元测试，固定商品同步确认、报告导入、状态更新、AI 详情、价格/库存同步、售价更新和更新文件 handler 契约；`pytest` 当前 371 passed，总覆盖率 `87.93%`，`operation_handlers.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 扩展查询 CLI handler 单元测试，固定统计总览、待发品统计、最近发品、品类列表的成功/空结果/异常契约；`pytest` 当前 379 passed，总覆盖率 `89.72%`，`query_handlers.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 扩展品类 CLI handler 单元测试，固定模板更新/矫正、Giga 品类同步确认与导出、CSV 映射更新和新品类导出契约；`pytest` 当前 397 passed，总覆盖率 `91.48%`，`category_handlers.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 扩展发品 CLI handler 单元测试，固定交互返回/选择、成功/失败结果和系统异常契约；`pytest` 当前 401 passed，总覆盖率 `91.81%`，`listing_handlers.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 扩展 CLI 菜单和任务分发单元测试，固定无效菜单、键盘中断、异常兜底和发品任务 CLI 分发契约；`pytest` 当前 405 passed，总覆盖率 `92.12%`，`menu.py` 与 `task_dispatcher.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 扩展模板解析器失败路径单元测试，固定工作簿打开失败、缺工作表、缺表头、无 Valid Values、异常兜底和变体主题 fallback 契约；`pytest` 当前 414 passed，总覆盖率 `92.85%`，`amz_template_parser.py` 覆盖率 96%。
- ✅ **2026-05-05 / Codex**: 扩展数据映射 helper 编排单元测试，固定配置加载/失败、空输入、LLM 增强成功/失败和 wrapper 委托契约；`pytest` 当前 420 passed，总覆盖率 `93.27%`，`data_mapping_helper.py` 覆盖率 85%。
- ✅ **2026-05-05 / Codex**: 扩展 Excel 生成器边界单元测试，固定默认路径发现、空输入、保存异常、重复表头解析和缺少硬编码字段告警契约；`pytest` 当前 425 passed，总覆盖率 `93.72%`，`excel_generator.py` 覆盖率 98%。
- ✅ **2026-05-05 / Codex**: 扩展模板管理服务失败路径单元测试，固定模板文件缺失、保存失败、报错解析失败、无模板记录、无需更新、更新失败和异常回滚契约；`pytest` 当前 432 passed，总覆盖率 `94.35%`，`amz_template_management_service.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 新增品类映射 CSV 更新器单元测试，固定文件缺失、读取失败、空文件、校验错误、错误截断、部分更新告警和更新异常契约；`pytest` 当前 439 passed，总覆盖率 `94.78%`，`category_mapping_csv_updater.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 扩展报告导入与状态更新服务单元测试，固定状态更新 0/-1 分支、CSV/TSV 读取、编码失败和清洗去重契约；`pytest` 当前 444 passed，总覆盖率 `95.13%`，`ListingStatusManager` 与 `AmzFullListImporterService` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 扩展单字段映射器兜底分支单元测试，固定 item dimension、category lookup、未知 source type、JSON path、未知单位和 item weight 兜底契约；`pytest` 当前 446 passed，总覆盖率 `95.32%`，`data_field_mapper.py` 覆盖率 100%。
- ✅ **2026-05-05 / Codex**: 升级 CI checkout action 到 Node 24 运行时，`.github/workflows/ci.yml` 从 `actions/checkout@v4` 调整为 `actions/checkout@v6`；GitHub Actions 日志确认 runner `amz-listing-runner-01` 当前版本 `2.333.1`，满足 checkout v6 兼容要求。
- ✅ **2026-05-05 / Codex**: 补齐生产初始迁移覆盖，Alembic 首次建库现在会创建 `ds_api_product_details`、`product_final_prices`、`amazon_cat_templates` 等运行时表，并在读取历史 SQL 时跳过宿主机专属 owner 语句；共享 PostgreSQL 临时库验证迁移成功且关键表齐全。
- ✅ **2026-05-05 / Codex**: 生产服务已安装到 `/data/docker-compose/amz-listing-management-system/`，镜像 `amz-listing-management-system:2026-05-05`，容器 `amz-listing-management-system` 当前 `running healthy`；共享 PostgreSQL 独立库 `amz_listing` 已执行 Alembic 迁移，HTTP socket、关键表和 `list-categories` smoke 均通过。
- ✅ **2026-05-05 / Codex**: 已将可继续使用的 Giga 与 LLM 凭据注入生产 `.env` 并重建容器；配置加载、Giga token、Giga `product_list` 只读接口、Qwen、DeepSeek、统一 LLM service、HTTP socket 和 `list-categories` 全部冒烟通过。
- ✅ **2026-05-16 / Codex**: 完成用户侧生产就绪收口：RDS 数据已迁入本机 `amz_listing`；Web 发品台开放 `https://amz-listing.meowy.fans` 并挂 BasicAuth/安全头/限流；首页增加生产就绪面板；发品品类固定为已有模板覆盖的 `CABINET` / `HOME_MIRROR`；CLI 与 Web 统一大小写归一后的待发品统计口径。验证：容器 `healthy`，`/api/readiness` 返回 ready，`pending-statistics` 为 CABINET 35 / HOME_MIRROR 21 / 其他 85，`list-categories` 返回 2 个可发品类，`pytest -q` 通过。
- ✅ **TASK-012**: 完成 `pydantic-settings` 迁移，重构了 `main.py`, `db_pool.py`, `logging`, `llm`, `giga` 等模块。
- ✅ **TASK-011**: 配置了 Pre-commit Hooks。
- ✅ **TASK-010**: 完成 Alembic 数据库迁移工具配置。

## 下一步计划
- ✅ EPIC-AMZ-LISTING-API-NATIVE-QUALITY：本轮 API-native 多类目发品质量升级 Epic 已完成，范围和验收见 `docs/epics/api-native-listing-quality-pipeline.md`。
- ✅ Epic 开发默认决策已确认：LLM review 最多自动重生成 1 次；Reviewer 采用保守策略；发品库存超过上限时 clamp 并审计；strict dry-run 显式启用；所有默认属性必须带 evidence/confidence，低置信 required 阻断 LIVE。
- ✅ API Attribute Resolver Phase 1：以 CABINET + OTTOMAN 为样本，把 API 发品属性构建从类目 hardcode 迁移到 Product Type Definitions schema + 配置化 attribute rules + evidence/confidence 输出。
- ✅ LLM 内容生成双阶段 Review Gate：Generator LLM 生成内容后，先 deterministic scanner，再由 Reviewer LLM 审查事实准确性、Amazon 规范和敏感 claim；只允许 review passed 内容进入 API 发品。
- ✅ strict dry-run：`generate-listing-api --strict-validation` 支持 Amazon `VALIDATION_PREVIEW` 预检，不 PUT，并将 Amazon issues 持久化为可审计反馈。
- ✅ SKU 级发品范围控制：`generate-listing-api` 支持 `--sku` / `--sku-file` / `--only-not-on-amazon`，本次运行返回并打印 scope/status audit summary。
- ✅ CABINET strict validation P0 属性修复：`door`、variation theme valid value、dimension/weight unit 和 CABINET 不适用属性输出已修复，复跑 18-SKU 后 Amazon preview ERROR 清零。
- ✅ CABINET 剩余 business blocker 收敛：Commercial Gate 库存超限已改为 clamp+审计，parent quantity 固定 0，CABINET 尺寸质量规则配置化，variation `Size`/`Color/Size` 的 `item_width` 渲染与 fail-closed 已补齐，combo 空间尺寸和 `Malaysia` country 映射已修复；最终 18-SKU strict dry-run 无 PUT，结果为 9 skipped existing、3 duplicate variation blocked、4 validation passed、4 Amazon width warning issues。
- ✅ OTTOMAN API-native resolver 回归验收：补 OTTOMAN variation resolver 配置、`Color`→`COLOR` 通用渲染、`fabric_type` 规则和 `remove_attributes` 配置；生产容器临时目录 strict dry-run 无 PUT，1 parent `VALIDATION_PREVIEW` passed 0 issues，2 child 因已存在被实时查重跳过。
- ✅ CABINET 48in vanity LIVE 前业务决策：48in vanity 可继续 strict dry-run 收集 Amazon warning，但 CABINET width >42in 的本地 finding 已配置为 `live_blocking`，LIVE PUT 前阻断；duplicate variation signatures 继续 fail-closed。
- 🔲 Phase B（待 Giga/标发权限就绪）：Giga 自动下单 → 物流追踪 → Amazon `confirmShipment` 回传
- 🔲 接入 Notifications API `ORDER_CHANGE` 替代纯轮询（需 Amazon 权限）
- 🔲 文档健康度债务：`docs_audit.py` 当前无断链、README 任务数已一致，但仍有 70 个 service 模块缺少 `docs/module-design/*.md`，已登记 TASK-136 分批收敛。
- ✅ **2026-06-09 / Codex**: 完成 confirmation listing issue 推荐属性自动补全独立流程：`sync-confirmation-listing-issues` 从价格/库存 delayed confirmation 审计中同步每个 SKU 最新 `getListingsItem` issues 入 `amazon_listing_issues`；`repair-listing-issues` 默认只处理 `price_inventory_confirmation` 来源的 open issue，第一版仅对高置信 `18448 recommended_uses_for_product` 生成/提交 PATCH；`confirm-listing-issue-repairs` 30 分钟后复核原 issue 是否消失并记录 `repair_confirmed`/`repair_failed`。目标测试 32 passed，全量 `pytest -q` 通过，当前测试数 620。
- ✅ **2026-06-09 / Codex**: 已部署 recommendation attribute repair 到生产并完成真实闭环验证。生产缓存 `CABINET` Product Type schema 后，dry-run 识别 95 个 `18448 recommended_uses_for_product` 可安全 PATCH、74 个非白名单 issue 保持 manual；live PATCH 提交 95 个，30 分钟后 `confirm-listing-issue-repairs` 确认 95/95 `repair_confirmed`。随后补充防止旧 delayed confirmation 快照重开已修复 issue 的保护并重部署；再次运行 `sync-confirmation-listing-issues` 只同步 74 个 manual issue，`recommended_uses_for_product` open issue 为 0。全量 `pytest -q` 通过，当前测试数 622。
- 🔲 部署前将 `selling-partner-definitions-prod-iad.s3.amazonaws.com` 加入 ECS `amazon-spapi-proxy.service` allowlist，以支持 Product Type Definitions schema 刷新和自动属性修复。
- 🔲 生产执行 Alembic `003_amazon_listing_issues` 后，先以 `LISTING_ISSUE_REPAIR_DRY_RUN=true` 运行 `sync-listing-issues`，人工复核 action 队列后再开启 live PATCH。
- 🔲 运营确认图片复核 SOP 后，可将 `LISTING_QUALITY_REQUIRE_IMAGE_REVIEW=true` 开启为发品阻断；同时复核 60in bathroom vanity 是否需要 product type / 尺寸字段映射调整。
- 🔲 补充 HOME_MIRROR 品类 SP-API 映射覆盖（`sp_api_home_mirror.json`）
- 🔲 用户业务确认 31 个未映射供应商品类应映射到哪个 Amazon 模板
- 🔲 Phase 5：Feeds API 批量提交 + Notifications API 自动监控
- 🔲 Grey SKU 价格数据同步（当前 `giga_product_base_prices` 中无记录）
- ✅ 当前已消除本轮识别出的 300+ 行文件规模预警。

## 风险与阻塞
- Amazon SP-API 生产调用被要求固定通过上海阿里云 ECS / 堡垒机出口；Amazon client 必须显式使用 `AMAZON_HTTPS_PROXY`，生产缺失代理配置时应 fail closed，禁止隐式直连。
- 当前覆盖率 `95.32%`，已越过 80% 覆盖率线；CLI 入口层覆盖率已收敛至 100%，核心模板解析/模板管理/映射/Excel/CSV/报告导入边界已明显收敛。
- GitHub Actions self-hosted runner 已注册并 online；runner `amz-listing-runner-01` 当前版本 `2.333.1`，已将 checkout action 升级到 `actions/checkout@v6` 以消除 Node.js 20 runtime deprecation。
- service 层直接 stdout 已基本收敛到统一 reporter；`amz_template_parser.py` 的 `_log_and_print` 仅写 logger，名称命中 `rg "print\\("` 但不输出 stdout。
- `main.py` 已降至 96 行，入口层拆分目标已完成；业务 service 和 repository 侧本轮识别出的 300+ 行文件规模预警已全部消除。
- 生产 `.env` 已生成数据库密码并注入 Giga/LLM 凭据；当前服务未通过 Traefik 暴露公网入口。Giga `product_list` 使用 `limit=1` 会返回格式错误，生产服务默认 `limit=100` 参数已验证可用。
