### [2026-05-16 21:17] [Codex]
完成生产数据迁移：从阿里云 RDS PostgreSQL `ecommerce` 通过临时 `postgres_fdw` schema `rds_import` 导入本机共享 PostgreSQL `amz_listing`。迁移前目标库备份为 `/home/liangqinhao/amz_listing_pre_rds_20260516.dump`；迁移保留 `alembic_version`，按目标表字段显式插入以兼容源库额外 `updated_at` 字段；迁移后删除临时 FDW schema/server/user mapping，未记录 RDS 密码。验证：目标表行数与源库一致，`view-statistics` 显示 Amazon 420、Giga 404、LLM 404、SKU 映射 476、价格 404、库存 404；待发品 141；`recent-listings` 与 `/diagnostics` 冒烟通过。

### [2026-05-16 21:33] [Codex]
完成用户侧生产就绪收口：新增 Web 首页 `/api/readiness` 就绪面板，发品品类改为已有模板覆盖的 `CABINET` / `HOME_MIRROR` 下拉选择；CLI 待发品统计和可用品类列表统一大小写归一口径；生产 Traefik 路由开放 `https://amz-listing.meowy.fans` 并挂 `security-headers`、`rate-limit`、`auth-basic`。当前可发品类统计：CABINET 35、HOME_MIRROR 21、其他/未映射 85；31 个未映射供应商品类需业务判断后再扩大发品范围。验证：容器 `healthy`，公网无认证返回 401，内部首页和 `/api/readiness` 正常，项目 `pytest -q` 通过。
