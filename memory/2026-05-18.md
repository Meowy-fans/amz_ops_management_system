
### [2026-05-18 01:00] [Codex]
完成 Phase A-E 多源标准化发品流程改造的基础设施：`StandardProduct` data class + `GigaProductNormalizer` (16 tests)；`AmazonSchemaService` schema 缓存/校验/合法值 (8 tests)；`discover-product-type` + `suggest-category-mappings` CLI 任务；`ProductContentGenerator` 品类感知 LLM 增强 (9 tests)；变体 API 映射 (`parentage` + `child_parent_sku_relationship` 后处理)。`pytest -q` 512 passed。

### [2026-05-18 01:30] [Codex]
开发规范偏移修复：红线级抽取 `AmazonProductTypeSchemaRepository`；拆分 `_convert()` 104→5 个 group handler；`product_content_generator.py` 320→284 行 + 提取 `content_validator.py`；`amazon_attribute_mapper.py` 318→298 行；修复死 logger/export/类型注解。

### [2026-05-18 16:50] [Codex]
OTTOMAN Storage Bench 全链路端到端验收：选择 Giga 新品 W48746798 (Beige) + W48764884 (Grey)，Giga Sync → generate-details → SKU Mapping (meow2511088jSUW / meow260518LZZCw) → Category Discovery (Storage Benches → OTTOMAN) → Schema Cache (8 required + 24 enforced) → LLM Content (0 warnings) → Attribute Mapping → VALIDATION_PREVIEW (3/3 VALID) → putListingsItem (5/5 ACCEPTED, 0 issues)。

验收结果：32/32 fields 数据一致，Beige price=$123.34 (formula: 88.76×1.07/0.77)，Grey price=$123.34 (same product)，Beige qty=0 (断货) / Grey qty=40 (现货)，全部与 Giga 源数据一致。价格第一轮硬编码 $89.99 错误，修正后重提交一致。

关键发现：OTTOMAN 使用 `frame[].material` 而非 `frame_material`、`variation_theme` 需 `name` 而非 `value` 子字段、seat/top/base 为嵌套对象、selector 字段需 `language_tag`。创建 `sp_api_ottoman.json` 覆盖配置。验收文档 `docs/acceptance/ottoman-e2e-2026-05-18.md`。
