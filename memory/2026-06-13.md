### [2026-06-13 23:13] [Codex]
本轮 API-native 多类目发品质量升级拆为独立 Epic `EPIC-AMZ-LISTING-API-NATIVE-QUALITY`，并完成前 4 个可独立验收任务：TASK-123 Excel 新品发品入口 deprecated，TASK-126 LLM 内容生成双阶段 Review Gate，TASK-124 配置化 Attribute Resolver v1，TASK-125 strict dry-run + Amazon `VALIDATION_PREVIEW` 预检闭环。

TASK-125 实现细节：`generate-listing-api --strict-validation` 在 dry-run 下执行实时 `getListingsItem` 查重和 Amazon `VALIDATION_PREVIEW`，不会调用 PUT；预检结果写入 `amazon_api_submissions`，状态区分 `validation_preview_passed`、`validation_preview_issues`、`validation_preview_failed`、`skipped_existing`；CLI 阻断 `--strict-validation --no-dry-run`。全量 `.venv/bin/pytest -q` 通过，668 tests collected。

### [2026-06-14 05:17] [Codex]
完成 TASK-128：SKU 级发品范围控制与审计摘要。`generate-listing-api` 现在支持 `--sku`（可重复/逗号分隔）、`--sku-file`、`--only-not-on-amazon`；新增 `ListingScope` / `ProductListingScopeFilter`，候选集先按本地 eligible + category 映射过滤，非 eligible/类目不匹配记录为 `blocked_scope_filter`，Amazon 已存在 SKU 记录为 `skipped_existing_scope`，非 404 查询错误 fail closed。`ProductListingService.generate_listings_via_api()` 返回 `audit.scope` 与 `audit.result_status_counts`，CLI 打印状态摘要。验证：scope/service/CLI 目标测试 46 passed，全量 `.venv/bin/pytest -q` 通过，676 tests collected。
