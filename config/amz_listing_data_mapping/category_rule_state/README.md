# Category Rule State

Per-category onboarding state manifests written by `onboard-category-v2`.

Each file: `{category}.json` — last run timestamp, pool SKUs, offline/preview KPI summary,
review blocking count, and paths to YAML / acceptance reports.

Do not edit by hand unless recording an operator override.
