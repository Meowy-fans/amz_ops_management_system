### [2026-06-25 22:35] [Cursor]
完成 S1 Amazon Schema 深合并开发验证：
1. `AmazonSchemaService._merged_properties` 从浅 `dict.update` 改为递归深合并，避免 `allOf[].then/else` 的部分条件约束覆盖 root 完整属性结构；`required` 列表取并集，`enum` 等非白名单 list 不盲目 union。
2. 新增 schema merge golden 夹具：从生产缓存导出 17 个 product type schema（2422 属性），固定深合并相对 legacy 浅合并 224 处 shape 升级、0 处降级；CHAIR `frame`/`seat` 从 `value` 恢复为 `object`，`maximum_weight_recommendation` 保持 `measure`。
3. 重生成 8 个 dry_run 新品类 YAML；diff 仅含预期 shape 升级（如 `frame`/`seat`/`top`/`tire` → `object/passthrough`，`color` → `list_value`）与 object 属性 fail-closed 调整。
4. 验证：targeted tests 22 passed，全量 `.venv/bin/pytest -q` 通过，`git diff --check` 通过。尚未部署生产。

### [2026-06-25 22:30] [Cursor]
完成 S1 Amazon Schema 深合并生产部署与验收：
1. 项目部署模板与生产 compose 镜像 tag 升级为 `amz-listing-management-system:2026-06-25-schema-deep-merge`，通过 `deploy/production/deploy.sh` 构建并重建主容器与 3 个 scheduler。
2. 生产验收：主容器 healthy，`amz-order-sync-scheduler` / `amz-order-daily-report-scheduler` / `amz-price-inventory-scheduler` running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302，`list-categories` 正常。
3. S1 专项验收：容器内 schema smoke 确认 CHAIR `frame=object`、`seat=object`、`maximum_weight_recommendation=measure`；`meow2511081Gqqd` strict dry-run 未 PUT，按 `blocked_attribute_coverage` fail-closed。
4. 生产镜像未安装 pytest，容器内测试不可用；宿主机 targeted tests 22 passed，`git diff --check` 通过。

### [2026-06-25 23:20] [Cursor]
完成 Confidence Review Pipeline 开发（未部署）：
1. 新增 evidence-grounded `ConfidenceScorer` 与 `config/listing_gates/review_policy.yaml`，默认先对白名单 `CHAIR` 灰度；支持 evidence 命中、长度、枚举合法性、LLM 自评与历史准确率加权。
2. `AttributeResolver` 支持 overrides，并为 required LLM 输出 `review_status` / `review_route` / `confidence_score` / `review_context`；resolver 不再直接把 required LLM 写成 `blocking=True`。
3. `AmazonListingAttributeCoverageGate` 改为未审核 required LLM 返回 `NEEDS_REVIEW_REQUIRED_ATTRIBUTE`，仅 `auto_approved` / `completed` 放行；object/measure 与缺必填仍 fail-closed。
4. 新增 `amz_listing_pending_review` Alembic migration、pending review repository、`ReviewManager`、`AttributeReviewAgent`、`AttributeReviewPlanRouter`、`review-pending-attributes` / `submit-reviewed-plans` CLI。
5. 验证：targeted tests 48 passed，相关回归 56 passed，Alembic `upgrade head --sql` 通过，新增/核心文件 ruff 通过，全量 `.venv/bin/pytest -q` 通过，`git diff --check` 通过；报告见 `docs/test-reports/2026-06-25-confidence-review-pipeline-dev.md`。

### [2026-06-25 23:55] [Cursor]
Confidence Review Pipeline code review + 测试收尾：
1. 修复 `attribute_resolver._apply_review_policy` 未注入 scorer 时 per-attribute 重建 `ConfidenceScorer`（每属性读 yaml）的问题，改为构建后缓存 `self.confidence_scorer`。
2. 修复 `_override_resolution` 经 `_finish` 覆盖 `state=review_completed` 的语义问题：override 路径直接返回、跳过 source 链与 `_finish`，保证 resume 不重跑 LLM。
3. 补测试缺口：新增 repository `list_reviews`/`save_decisions` SQL 合约测试与 resolver override 单测；新模块覆盖率 repository 96% / scorer 87% / plan_router 81% / review_manager 80%（总 85%，均达标）。
4. 验证：目标模块 51 passed，改动文件 ruff All checks passed，全量 `tests/unit tests/integration` 回归全部通过，无回归。
5. 结论：实现正确、fail-closed 保持；object/measure required LLM 走 human 落 pending review（仍不自动发品）属合理设计，含 needs_human 行停在 in_progress 等人工录入入口为 P4 分阶段 scope。

### [2026-06-25 23:10] [Cursor]
完成 Confidence Review Pipeline 生产部署与验收：
1. 生产 compose 镜像 tag 从 `2026-06-25-schema-deep-merge` 升级为 `2026-06-25-confidence-review-pipeline`，通过 `deploy/production/deploy.sh` 构建并重建主容器与 3 个 scheduler。
2. 执行 Alembic `010_amz_listing_pending_review`，确认 `amz_listing_pending_review` 表存在、初始业务 rows=0。
3. 生产 smoke：主容器 healthy，3 个 scheduler running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302，`list-categories` 正常；`review-pending-attributes --category CHAIR` 与 `submit-reviewed-plans --category CHAIR` dry-run 正常。
4. Confidence review 闭环 smoke：临时记录完成 pending 落库 -> `AttributeReviewAgent` evidence 命中审核 -> `completed` -> 清理，清理后残留 0 条。
5. CHAIR 单 SKU `meow2511081Gqqd` dry-run 未 PUT，LLM 连续提取成功，最终仍因真实非审核类属性缺口 `blocked_attribute_coverage` fail-closed；主容器近期日志无 `JSON解析失败` / `无效JSON响应` / `Traceback` / `ERROR`。
6. 同步更新 `STATUS.md`、`TODO.md`、部署验收报告、`/data/README.md` 与 `/data/TODO.md`。
