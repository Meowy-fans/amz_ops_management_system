### [2026-06-08 03:40] [Codex]
按开发规范完成内容生成模块主链路优化：`generate-details` 从旧 `prod_detail_gen_amz` 切到 `ProductContentGenerator` / `prod_detail_gen_v2`，先用 `GigaProductNormalizer` 将 Giga raw_data 转为 `StandardProduct`，再生成品类感知标题、5 点、描述、search_terms、generic_keyword；`ds_api_product_details` 表结构不变，`raw_json` 记录 v2 元数据、product_type、enriched_attributes、validation_warnings。新增 `LLMProductDetailRepository.get_product_type_for_sku()` 通过 `supplier_categories_map` 解析标准品类。验证：相关 30 tests passed；全量 `pytest -q` 553 passed。

### [2026-06-08 04:19] [Codex]
按 API-only 发品思路完成查重改造：`ProductListingRepository.get_pending_listing_skus()` 不再依赖 `amz_all_listing_report` 判断 Amazon 是否已发布，只保留本地 Giga 可发布资格（非 oversize、sellerType GENERAL、价格可用）；`AmazonListingSubmitter` 在非 dry-run 的 `VALIDATION_PREVIEW` / `putListingsItem` 前调用 Listings Items API `getListingsItem` 实时查重，404 才继续，200 记录 `skipped_existing`，查重异常记录 `failed_existing_check` 并阻断提交。验证：相关 29 tests passed。

### [2026-06-08 04:21] [Codex]
API-only 发品查重改造完成后补充验证：全量 `.venv/bin/pytest -q` 通过，`.venv/bin/pytest --collect-only -q` 汇总当前测试数为 555。

### [2026-06-08 04:38] [Codex]
完成 `generate-listing-api` API-native 主链路改造：新增 `src/models/amazon_listing.py`（`AmazonListingDraft`/content/offer/variation）、`AmazonListingDraftBuilder` 和 `AmazonListingPayloadBuilder`，API 发品路径直接从 `ProductDataRepository` 的商品数据构建 SP-API attributes plan，不再生成 Excel-like 中间行，也不再读取 `amazon_cat_templates` 模板规则；Excel 发品路径仍保留旧模板链路。Payload builder 优先使用 cached Product Type Definitions valid values 做枚举对齐，避免 dry-run 构建时外呼。验证：全量 `.venv/bin/pytest -q` 通过，当前测试数 562。

### [2026-06-08 07:59] [Codex]
落地图片资产框架第一版：新增 `product_image_assets` 表和 Alembic `004_product_image_assets`，建立 raw / auto_approved / needs_review / approved / rejected / processing_failed 状态模型；新增 `AmazonListingImageAssetRepository`、`AmazonListingImageInspectionService`、`AmazonListingImageAssetService`、`AmazonListingImageSelector`。第一版只实现确定性自动检测（HTTPS、HEAD 可访问性、Content-Type、Content-Length、supplier-hosted 标记 needs_review），预留白底检测、OCR/水印、主体占比、抠图、对象存储、人工审核 UI。`generate-listing-api` 发品时只读取 approved/auto_approved 图片资产覆盖 raw 图片，不在发品过程中联网探测。验证：新增/相关 17 tests passed；`alembic upgrade head --sql` 通过；全量 `.venv/bin/pytest -q` 通过，当前测试数 569。

### [2026-06-08 08:18] [Codex]
完成 API 发品价格/库存 Commercial Gate 可审计守门：新增 `config/listing_gates/commercial_gate.yaml`、`amazon_listing_commercial_gate_runs` 表、Alembic `005_commercial_gate_runs`、`AmazonListingCommercialGate` 和 `AmazonListingCommercialGateRepository`。`ProductDataRepository.get_full_product_data()` 扩展 price_currency/cost_at_pricing/pricing_formula_version/price_updated_at/inventory_quantity/buyer_qty/seller_qty/inventory_last_updated；Amazon 发布库存口径从 `quantity + buyer_qty` 改为明确的 `giga_inventory.quantity`，buyer/seller qty 只进入审计快照。`generate-listing-api` 在构建 payload 前执行 Commercial Gate，passed/blocked 都落审计表；blocked SKU 返回 `blocked_commercial_gate`，不进入 submitter。验证：新增/相关 22 tests passed；`alembic upgrade head --sql` 通过；全量 `.venv/bin/pytest -q` 通过，当前测试数 577。

### [2026-06-08 09:07] [Codex]
完成 API 发品变体解析与增量子体支持：新增 `config/listing_gates/variation_theme_strategy.yaml`、`amazon_variation_resolution_runs` 表、Alembic `006_variation_resolution_runs`、`AmazonVariationResolver` 和审计仓库；新家族按 uniqueness/buyer_relevance/data_confidence/simplicity/coverage 选择 variation theme，已发布父体增量 child 通过 Giga `associateProductList` 找历史 meowy SKU，继承既有 parent/theme，并检查 child 属性组合唯一性。重复或缺失属性会返回 `blocked_variation_resolution`，不生成 payload。同步拆出 `ProductListingAPIPlanBuilder`，使 `ProductListingService` 从 685 行降至 343 行。验证：新增/相关 28 tests passed；`alembic upgrade head --sql` 通过；全量 `.venv/bin/pytest -q` 通过，当前测试数 587。

### [2026-06-08 09:22] [Codex]
完成发品提交后确认闭环：`AmazonListingSubmitter` 在真实 `putListingsItem` 返回 `ACCEPTED` 且 `issues=[]` 后，会立即调用 `getListingsItem` 做提交后确认。新增状态 `listing_confirmed`、`confirmed_with_issues`、`accepted_pending_confirmation`、`confirmation_failed`、`not_accepted`；确认失败不会把 PUT accepted 改写为发品失败，审计记录保存 `put_response`、`confirm_response` 或确认错误。`dry_run` 和 `validation_only` 不做提交后确认。验证：submitter 12 tests passed；相关 29 tests passed；全量 `.venv/bin/pytest -q` 通过，当前测试数 592。

### [2026-06-08 09:44] [Codex]
完成价格/库存 API 更新 API-native 改造：新增 `amazon_listing_items_cache` 表、Alembic `007_amazon_listing_items_cache`、`AmazonListingItemCacheRepository` 和 `AmazonPriceInventoryUpdateService`。`update-price-inventory-api` 先用 `searchListingsItems` 刷新 API cache，再从 `amazon_listing_items_cache` + `meow_sku_map` 生成候选，不再依赖 `amz_all_listing_report`；每个 SKU PATCH 前 `getListingsItem` 实时确认、ERROR issue 阻断、no-change diff，只 PATCH 变化字段；PATCH 后再次 `getListingsItem` 对比目标价格/库存，输出 `update_confirmed`、`confirmed_with_mismatch`、`confirmed_with_issues`、`accepted_pending_confirmation`、`confirmation_failed` 等状态。验证：相关 19 tests passed；`alembic upgrade head --sql` 通过；全量 `.venv/bin/pytest -q` 通过，当前测试数 594。

### [2026-06-08 09:47] [Codex]
价格/库存 API 更新路径已改为 API-native：新增 amazon_listing_items_cache 缓存和仓储，通过 searchListingsItems/getListingsItem 刷新 Amazon 事实源，候选 SKU 改为 cache + meow_sku_map，不再依赖 amz_all_listing_report。更新前做 getListingsItem、ERROR issue 阻断和 no-change 跳过，实际写入用 patchListingsItem，ACCEPTED 后再 getListingsItem 确认，确认 mismatch 明细写入提交审计记录。相关 19 个目标测试和全量 pytest 均通过。

### [2026-06-08 09:54] [Codex]
完成价格/库存 API 更新每小时生产调度：`deploy/production/docker-compose.yml` 新增 `amz-price-inventory-scheduler` 服务，复用业务镜像执行 `scripts/hourly_price_inventory_update.sh`，默认每 3600 秒运行一次 `python main.py --task update-price-inventory-api --no-dry-run`，首次延迟默认 120 秒。新增 `PostgresAdvisoryLock`，`handle_update_price_inventory_api` 执行前获取 PostgreSQL advisory lock，避免定时任务与手工任务重叠；异常会重新抛出，便于 CLI/调度日志识别失败。验证：目标 27 tests passed，脚本语法通过，临时 compose config 校验通过，全量 `.venv/bin/pytest -q` 通过，当前测试数 600。

### [2026-06-08 10:00] [Codex]
实际部署价格/库存每小时 scheduler 时，已将生产 compose 同步到 `/data/docker-compose/amz-listing-management-system/docker-compose.yml`，但 `docker compose build` 在下载 Python 依赖 pandas metadata 时发生 files.pythonhosted.org 读取超时，镜像未构建成功。随后已在 Dockerfile 增加 `PIP_DEFAULT_TIMEOUT=120` 和 `PIP_RETRIES=5`，但重新执行 Docker build 需要 Docker socket 提权，审批系统因使用额度限制拒绝，部署停在构建前，生产仍运行旧容器且 scheduler 尚未启动。

### [2026-06-08 20:51] [Codex]
完成价格/库存每小时更新生产部署：生产 compose 已同步并启动 `amz-listing-management-system:2026-06-08` 主服务和 `amz-price-inventory-scheduler`，scheduler 每小时执行 `update-price-inventory-api --no-dry-run`，advisory lock 验证有效（手工任务运行时 scheduler 自动跳过）。部署过程中修复两个真实环境问题：1) `searchListingsItems` 带 pageToken 翻页时保留 issueLocale/pageSize/includedData 等原搜索参数，避免 Amazon 返回 `Search parameters do not match the provided pageToken`；2) 新增 Alembic `008_giga_base_price_sku_unique`，去重并为 `giga_product_base_prices.giga_sku` 建唯一索引，修复 Giga 价格同步 `ON CONFLICT(giga_sku)` 失败。生产验证：主容器 healthy，scheduler running，Alembic current 为 `008_giga_base_price_sku_unique (head)`；手工真实执行完成 273 SKUs processed，Failed 0；最近 273 条提交状态为 skipped_no_change=16、blocked_listing_issue=11、confirmed_with_issues=127、confirmed_with_mismatch=119。已同步更新 `/data/README.md` 和 `/data/TODO.md` 服务清单/变更日志。

### [2026-06-08 20:59] [Codex]
按用户要求导出最近一次生产价格/库存 API 更新审计记录：明细 CSV 位于 `output/audit_exports/latest_price_inventory_update_audit_20260608_2049.csv`，生产挂载副本位于 `/data/volumes/amz-listing-management-system/output/latest_price_inventory_update_audit_20260608_2049.csv`；汇总 CSV 位于 `output/audit_exports/latest_price_inventory_update_summary_20260608_2049.csv`。明细共 273 条，字段包含本地目标价格/库存、Giga 价格/库存更新时间、PATCH 目标、Amazon 确认价格/库存、mismatch、issue 摘要、request/response JSON 和处理依据。状态分布：confirmed_with_issues=127、confirmed_with_mismatch=119、skipped_no_change=16、blocked_listing_issue=11。

### [2026-06-08 23:40] [Codex]
完成价格/库存 API 更新 30 分钟二次确认：新增 confirm-price-inventory-api 和 AmazonPriceInventoryDelayedConfirmationService，scheduler 每轮先执行 delayed confirmation 再执行 update-price-inventory-api；二次确认只调用 getListingsItem，不 PATCH，并写入 operation=delayed_confirmation 的 amazon_api_submissions 记录，通过 response_body.source_submission_id 关联原 submission。生产部署完成，手工执行处理 480 条历史记录：delayed_update_confirmed=230、delayed_confirmed_with_issues=247、delayed_confirmed_with_issues_and_mismatch=2、delayed_confirmed_with_mismatch=1；submission_id=1123 已 delayed_update_confirmed 且 mismatches={}。验证：相关 46 tests passed、全量 pytest 通过、当前测试数 608。

### [2026-06-08 23:54] [Codex]
审计价格/库存 API 更新二次确认 issues：当前最新 delayed_confirmation 中 127 个 SKU 仍有 issues，共 331 条 issue，全部为 WARNING、无 ERROR；按 code 聚合为 18448 缺少推荐属性 217 条/111 SKU（主要 recommended_uses_for_product、installation_type、style）、100335 CABINET item_depth_width_height 超 Amazon 最大值规则 80 条/41 SKU、18367 Amazon 将 product_type 从 CABINET 自动调整为 SINK 34 条/17 SKU。amazon_listing_issues 当前无 open 记录，说明这批 issues 来自价格/库存 getListingsItem confirmation 响应。多数 listing status 仍包含 BUYABLE/DISCOVERABLE，但部分仅 DISCOVERABLE，需要后续按属性补全、尺寸/品类模板规则、product_type 归因分别设计处理。
