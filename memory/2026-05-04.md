### [2026-05-04 20:56] [Codex]
启动 amz_listing_management_system 偏移缓解 Phase 0：修复 `.gitignore` 误屏蔽 `tests/` 下正式单测文件的问题；修复 `test_sync_all_inventory_success` 因 mock `ThreadPoolExecutor` 配合 `as_completed()` 导致完整 pytest 挂起的问题。当前基线：`pytest` 为 52 passed，覆盖率 `pytest --cov=src --cov=infrastructure` 为 38%。

### [2026-05-04 20:56] [Codex]
新增 GitHub Actions CI 配置 `.github/workflows/ci.yml`，使用 self-hosted runner 执行依赖安装、`pytest`、覆盖率基线检查（当前 fail-under 35）。该配置待 push/PR 后由 runner 实测验证。

### [2026-05-04 20:56] [Codex]
补齐偏移缓解 Phase 1 的关键文档：`docs/module-design/product-listing-service.md`、`giga-sync-services.md`、`pricing-and-update-file.md`、`template-mapping.md`，以及 `docs/api-contracts/service-results.md`。文档按现有行为记录 Draft 契约，作为后续拆分 `main.py` 和 service 去展示化的基线。

### [2026-05-04 20:56] [Codex]
开始偏移缓解 Phase 3 前置改造：在 `main.py` 中新增 `_dispatch_task()`，合并 `main()` 非交互分发与 `run_task()` 分发的重复分支，同时保留 `generate-listing` 在 `run_task()` 中直接返回 service 结果的既有行为。改后 `pytest` 仍为 52 passed。发现 `main.py` 的交互菜单循环目前位于 `run_task()` 中不可达区域，已登记为后续入口结构风险。

### [2026-05-04 20:56] [Codex]
继续 Phase 3：修复 `main.py` 在无 `--task` 时不会进入交互菜单的问题；将菜单展示、菜单编号映射和交互循环拆到 `src/cli/menu.py`，通过 `session_factory` 与 `dispatch_task` 回调避免依赖业务 handler。`main.py` 从 937 行降至 843 行，`pytest` 仍为 52 passed。

### [2026-05-04 20:56] [Codex]
继续收敛入口层：将 `_dispatch_task()` 从长 `if/elif` 改为 `TASK_HANDLERS` 注册表，保留 `run_task(generate-listing)` 返回 service 结果的历史行为；为 `src/cli/menu.py` 新增 3 个单测。当前验证：`pytest` 55 passed，`pytest --cov=src --cov=infrastructure --cov-fail-under=35` 通过，总覆盖率 39%。

### [2026-05-04 20:56] [Codex]
继续拆分 `main.py`：将只读查询类 handler（数据统计、待发品统计、最近发品记录、品类列表）迁移到 `src/cli/query_handlers.py`，并新增 3 个单测覆盖 SQL 查询展示路径。当前 `main.py` 673 行；验证：`pytest` 58 passed，覆盖率 38%，CI 35% 基线通过。

### [2026-05-04 20:56] [Codex]
继续拆分 `main.py`：将模板更新/纠错、Giga 品类同步、CSV 映射更新和新品类导出迁移到 `src/cli/category_handlers.py`，并新增 4 个单测覆盖取消/非交互保护/空导出路径。当前 `main.py` 445 行，已低于 500 行红线；验证：`pytest` 62 passed，覆盖率 39%，CI 35% 基线通过。

### [2026-05-04 20:56] [Codex]
继续拆分 `main.py`：将亚马逊发品生成 CLI handler 迁移到 `src/cli/listing_handlers.py`，并新增 2 个单测覆盖无效交互选择和成功结果展示。当前 `main.py` 378 行；验证：`pytest` 64 passed，覆盖率 39%，CI 35% 基线通过。

### [2026-05-04 20:56] [Codex]
完成 Phase 3 的入口层拆分目标：将运营任务 handler 迁移到 `src/cli/operation_handlers.py`，将任务注册/分发迁移到 `src/cli/task_dispatcher.py`。`main.py` 当前 96 行，低于 150 行目标。新增 dispatcher 单测后，验证为 `pytest` 66 passed，覆盖率 42%，CI 35% 基线通过。

### [2026-05-04 20:56] [Codex]
启动 Phase 4 service 去展示化：为 `PricingService` 增加 `PricingProgressReporter` / `NullPricingProgressReporter`，将内部进度 `print()` 收敛到 reporter，默认 reporter 继续保持现有 CLI 输出不变；新增静默 reporter 单测。验证：`pytest` 67 passed，覆盖率 42%，CI 35% 基线通过。

### [2026-05-04 21:51] [Codex]
完成 Phase 4 主要同步服务输出边界收敛：新增 `src/services/progress_reporter.py`，为 `GigaSyncService`、`GigaInventorySyncService`、`GigaPriceSyncService` 注入可替换 reporter，默认保持 CLI 输出，测试可用 `NullProgressReporter` 静默；当前 `pytest` 71 passed，覆盖率 43.13%。

### [2026-05-04 22:00] [Codex]
继续收敛 service 层展示输出：`InventoryPriceUpdaterService` 支持可替换 reporter，并将同一 reporter 传递给 Giga 价格、Giga 库存和售价更新子服务；新增静默 reporter 单测。验证：`pytest` 74 passed，覆盖率 44.47%。

### [2026-05-04 22:06] [Codex]
继续收敛 service 层展示输出：`ProductDetailGenerationService` 支持可替换 reporter，默认 CLI 输出保持不变；新增无待处理 SKU 和分批处理静默 reporter 单测。验证：`pytest` 76 passed，覆盖率 45.53%。

### [2026-05-04 22:13] [Codex]
继续收敛 service 层展示输出：`CategoryMaintenanceService` 支持可替换 reporter，默认 CLI 输出保持不变；新增 Giga 品类同步和 CSV 映射更新的静默 reporter 单测。验证：`pytest` 78 passed，覆盖率 45.71%。

### [2026-05-04 22:17] [Codex]
将 `PricingService` 的本地 reporter 类收敛为统一 `src/services/progress_reporter.py` 的兼容子类，保留 `PricingProgressReporter` / `NullPricingProgressReporter` 导出以避免破坏现有调用。验证：`pytest` 78 passed，覆盖率 45.67%。

### [2026-05-04 22:11] [Codex]
继续收敛 service 层展示输出：`AmzFullListImporterService` 与 `ListingStatusManager` 支持可替换 reporter，默认 CLI 输出保持不变；新增导入事务、回滚、状态更新静默 reporter 单测。验证：`pytest` 82 passed，覆盖率 46.91%。

### [2026-05-04 22:18] [Codex]
继续收敛 service 层展示输出：`CategoryService` 支持可替换 reporter，`PricingService` 将同一 reporter 传入品类判定链路；新增品类判定静默 reporter 单测。验证：`pytest` 84 passed，覆盖率 47.50%。

### [2026-05-04 22:24] [Codex]
继续收敛 service 层展示输出：`TemplateManagementService` 支持可替换 reporter；交互输入提示改为 reporter 输出后再调用 `input()`，默认交互能力保留。验证：`pytest` 86 passed，覆盖率 47.62%。当前 service 层直接 stdout 基本收敛到统一 reporter，`amz_template_parser.py` 的 `_log_and_print` 仅写 logger。

### [2026-05-04 22:31] [Codex]
继续补核心发品链路测试：`ProductListingService` 新增品类无待发 SKU、缺模板、Excel 生成异常回滚、变体父子行与日志生成路径单测。验证：`pytest` 90 passed，覆盖率 49.10%；`ProductListingService` 覆盖率提升至 80%。

### [2026-05-04 22:37] [Codex]
继续补核心发品链路测试：`SkuMappingService` 新增空源 SKU、全已映射、创建新映射、插入异常回滚、SKU 生成冲突重试失败路径单测。验证：`pytest` 95 passed，覆盖率 50.06%；`SkuMappingService` 覆盖率提升至 93%。

### [2026-05-04 22:45] [Codex]
继续补核心发品链路测试：`VariationThemeService` 新增首轮 LLM 判定成功、重复属性二轮纠正、缺 prompt fallback、LLM 异常 fallback、尺寸格式化和 HTML 清洗单测。验证：`pytest` 101 passed，覆盖率 51.99%；`VariationThemeService` 覆盖率提升至 93%。

### [2026-05-04 22:53] [Codex]
继续补核心发品链路测试：`DataMappingHelper` 新增有效值对齐、字段引用、JSONB fallback、数组/组合计算、尺寸/重量/品类查找、LLM 增强与 fallback 单测。验证：`pytest` 106 passed，覆盖率 54.53%；`DataMappingHelper` 覆盖率提升至 77%。测试记录当前行为：`field_reference` 在有效值对齐前复制原值，只有模板约束字段会对齐。

### [2026-05-04 22:33] [Codex]
缓解文件规模红线：将 `DataMappingHelper` 的 LLM 增强逻辑拆到 `src/utils/data_mapping_llm.py`，原 `_enrich_with_llm()` 保留兼容委托；`src/utils/data_mapping_helper.py` 从 510 行降至 445 行。验证：`pytest` 106 passed，覆盖率 54.61%。

### [2026-05-04 22:33] [Codex]
更新架构文档以消除实现偏移：`docs/architecture.md` 已补当前 `main.py` 薄入口、`src/cli/*` CLI 分层、`ProgressReporter` 输出边界和 `data_mapping_llm` 映射增强模块。验证：`pytest` 106 passed。

### [2026-05-04 22:37] [Codex]
缓解 `ProductListingService` 文件规模预警：将变体父子行与变体发品日志构造逻辑拆到 `src/services/product_listing_variation_builder.py`，保留原 service 内部兼容包装；`product_listing_service.py` 从 500 行降至 369 行。验证：`pytest` 106 passed，覆盖率 54.92%。

### [2026-05-04 22:40] [Codex]
继续缓解文件规模预警：将模板规则矫正的 Amazon 报错报告解析与必填字段规则应用逻辑拆到 `src/services/amz_template_rule_correction.py`，`TemplateManagementService` 保留兼容委托；`amz_template_management_service.py` 从 475 行降至 389 行。验证：`pytest` 106 passed，覆盖率 55.00%。

### [2026-05-04 22:50] [Codex]
继续缓解文件规模预警：将品类映射 CSV 更新的文件读取、行验证、批量更新和输出统计流程拆到 `src/services/category_mapping_csv_updater.py`；`category_maintenance_service.py` 从 388 行降至 227 行。验证：`pytest` 106 passed，覆盖率 55.32%。

### [2026-05-04 22:53] [Codex]
继续缓解系统设计/开发规范偏移：新增 `src/utils/data_field_mapper.py`，将 `DataMappingHelper` 的单字段 source type 映射、JSONB 读取、单位换算和重量计算职责拆出，原 helper 保留兼容委托方法并降至 331 行。验证 `pytest` 106 passed，`pytest --cov=src --cov=infrastructure --cov-fail-under=35` 通过，总覆盖率 55.42%。

### [2026-05-04 22:57] [Codex]
继续缓解模板解析偏移：新增 `src/services/template_parser_helpers.py`，将 Data Definitions 表头识别、字段定义提取、Valid Values 解析等纯规则从 `AdvancedTemplateParser` 拆出；`amz_template_parser.py` 从 404 行降至 285 行。新增 `tests/unit/services/test_template_parser_helpers.py` 覆盖 helper 与最小 Excel 端到端解析；验证 `pytest` 109 passed，覆盖率 58.40%，`amz_template_parser.py` 覆盖率 71%。

### [2026-05-04 23:00] [Codex]
继续缓解模板管理偏移：新增 `src/services/template_variation_config.py`，将变体字段映射、优先主题用户输入/历史配置/default 选择职责从 `TemplateManagementService` 拆出；模板管理服务从 389 行降至 291 行。新增 `tests/unit/services/test_template_variation_config.py` 覆盖 mapping、输入、历史和默认路径；验证 `pytest` 114 passed，覆盖率 58.53%，新模块覆盖率 100%。

### [2026-05-04 23:08] [Codex]
继续缓解核心发品服务偏移：新增 `product_listing_config.py`、`product_listing_flow_helpers.py`、`product_listing_log_builder.py`，将品类配置加载、待发 SKU 按品类过滤、发品结果字典和日志 payload 构造从 `ProductListingService` 拆出；`product_listing_service.py` 从 369 行降至 294 行。新增 `tests/unit/services/test_product_listing_flow_helpers.py` 覆盖过滤、结果契约和日志构造；验证 `pytest` 118 passed，覆盖率 58.84%，`ProductListingService` 覆盖率 91%。

### [2026-05-04 23:11] [Codex]
继续缓解变体主题服务偏移：新增 `src/services/variation_theme_helpers.py`，将产品清洗、LLM prompt 组装、优先主题过滤、属性唯一性校验、尺寸属性格式化和 HTML 清洗从 `VariationThemeService` 拆出；服务从 333 行降至 229 行。新增 `tests/unit/services/test_variation_theme_helpers.py` 覆盖 helper 行为；验证 `pytest` 121 passed，覆盖率 58.93%，新 helper 覆盖率 98%。

### [2026-05-04 23:14] [Codex]
完成本轮 300+ 行文件规模预警清理：新增 `src/utils/data_mapping_valid_values.py` 和 `src/utils/data_mapping_tasks.py`，将有效值对齐、模糊匹配和 LLM task 提取从 `DataMappingHelper` 拆出；`data_mapping_helper.py` 从 331 行降至 261 行。新增 `tests/unit/utils/test_data_mapping_helpers_extracted.py`；验证 `pytest` 123 passed，覆盖率 59.33%。当前本轮识别出的 300+ 行文件预警已全部消除。

### [2026-05-04 23:17] [Codex]
继续治理全局文件规模偏移：新增 `src/repositories/giga_price_transform.py`，将 Giga 价格记录过滤、按 SKU/Giga 指数去重、base price row 和 tier price row 构造从 `GigaProductPriceRepository` 拆出；仓库文件从 386 行降至 277 行。新增 `tests/unit/repositories/test_giga_price_transform.py` 覆盖纯转换逻辑；验证 `pytest` 127 passed，覆盖率 60.60%，新 transform 覆盖率 93%。

### [2026-05-04 23:21] [Codex]
重新盘点系统设计/开发规范偏移：GitHub CLI 可访问仓库，但 GitHub API 显示 `actions/runners` 的 `total_count=0`，说明 `.github/workflows/ci.yml` 已存在但 self-hosted runner 未注册，CI 远端绿色验证仍阻塞。当前剩余偏移主要为覆盖率 60.60% 未达核心 80% 目标、集成测试不足、生产部署未迁移到 `/data/docker-compose/` 与共享 PostgreSQL 策略、TODO 早期状态少量不一致。

### [2026-05-04 23:39] [Codex]
为 `amz_listing_management_system` 新增 repo-level GitHub Actions runner：部署目录 `/data/docker-compose/github-runner-amz-listing/`，数据目录 `/data/volumes/github-runner-amz-listing/data`，容器 `github-runner-amz-listing`，runner 名称 `amz-listing-runner-01`，标签 `self-hosted`,`Linux`,`X64`,`ci`。GitHub API 验证 runner online；已清空 `.env` 中一次性 `RUNNER_TOKEN`，并同步 `/data/README.md`、`/data/TODO.md` 服务清单。当前远端 workflow 列表为空，CI green 验证等待本地 workflow 变更推送到 GitHub。

### [2026-05-05 00:20] [Codex]
完成 repo-level CI 闭环：AMZ runner 专用镜像 `infra-cicd/github-runner-amz-listing:2.333.1` 基于通用 runner 镜像构建，并预装 Python 3.12 / pip / venv；构建阶段清空 `BUILD_*_PROXY` 以规避本机代理访问 Ubuntu apt 的 502，运行阶段保留 `RUNNER_*_PROXY` 访问 GitHub。已推送 commit `c5dc219`，GitHub Actions run `25330003050` 在 `amz-listing-runner-01` 绿色通过。
