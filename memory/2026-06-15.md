
### [2026-06-15 05:57] [Codex]
完成 TASK-130 OTTOMAN API-native resolver 回归验收。补充 OTTOMAN variation resolver 配置、通用 Color→COLOR variation theme 渲染、OTTOMAN fabric_type attribute rule，以及 remove_attributes 配置以移除 Amazon preview 判定不适用的 item_type_name/item_depth/item_height/room_type/target_audience_base。生产容器临时目录 /tmp/codex-amz-task129 strict dry-run（无 PUT）：PARENT-2B500764CF03 validation_preview_passed，request_id=e9eefd4b-4754-408e-a140-07b81b5a9a68，issues=0；meow260518LZZCw / meow2511088jSUW 因已存在被实时查重 skipped_existing。报告：docs/test-reports/2026-06-15-ottoman-api-native-regression.md。全量 .venv/bin/pytest -q 通过。

### [2026-06-15 06:20] [Codex]
完成 TASK-131 Excel legacy retirement 收口。新增 docs/retirement/excel-listing-retirement-2026-06-15.md，明确 Excel 新品发品、Amazon Excel 模板解析/管理/纠错、template variation config、Excel row variation builder 的退役边界、替代 API-native 模块和 2026-07-31 删除窗口。为 Excel-only/legacy 模块与 generate-listing 兼容函数添加脚本可识别 @retire 标记；退役扫描识别 15 项待退役，废弃代码约 3.7%。目标 legacy 测试 81 passed。

### [2026-06-15 06:42] [Codex]
完成 TASK-134 CABINET 48in vanity LIVE 策略决策并落地。新增 docs/decisions/ADR-2026-06-15-cabinet-48in-live-policy.md：48in vanity 可继续用 CABINET strict dry-run / Amazon VALIDATION_PREVIEW 收集 warnings，但 CABINET width >42in 的 ISSUE_DERIVED_DIMENSION_RANGE 配置为 live_blocking，LIVE PUT 前由 AmazonListingSubmitter 提升为 blocked_quality_gate。unblock 路径是 Product Type Discovery 切换更合适 product type，或用证据更新阈值；duplicate variation signatures 继续 fail-closed。目标测试 23 passed。

### [2026-06-15 06:50] [Codex]
EPIC-AMZ-LISTING-API-NATIVE-QUALITY 已标记 Completed。TASK-123~134 全部完成；初次 CABINET 18-SKU strict dry-run 暴露的问题已由 TASK-132/TASK-133/TASK-134 收敛，OTTOMAN 回归通过，Excel legacy retirement 已独立成文并加 @retire 审计标记。最终验证：全量 .venv/bin/pytest -q 通过，pytest collect 692，git diff --check 通过，退役扫描识别 15 项待退役、废弃代码约 3.7%。

### [2026-06-15 06:55] [Codex]
完成 API-native 发品质量 Epic 生产部署。将 deploy/production/docker-compose.yml 与 /data/docker-compose/amz-listing-management-system/docker-compose.yml 镜像升级为 amz-listing-management-system:2026-06-15，执行 deploy/production/deploy.sh 构建并重建主服务与三个 scheduler。验证：主容器 healthy；公网入口返回 SSO 302；生产 list-categories 返回 CABINET/HOME_MIRROR；legacy generate-listing --category CABINET 已转发 API-native dry-run，无 Excel/无 PUT；OTTOMAN strict dry-run（meow2511088jSUW、meow260518LZZCw）1 parent validation_preview_passed、2 child skipped_existing、issues=0、无 PUT。/data/README.md 与 /data/TODO.md 已同步镜像版本和变更日志。

### [2026-06-15 07:12] [Codex]
按开发规范补齐本次部署文档：新增 docs/test-reports/2026-06-15-api-native-quality-production-deploy.md，更新 README.md、deploy/production/README.md、docs/epics/api-native-listing-quality-pipeline.md、docs/architecture.md、docs/decisions/001-api-native-listing.md、docs/retirement/excel-listing-retirement-2026-06-15.md、STATUS.md、TODO.md。docs_audit 无断链，但报告 70 个既有 service 模块缺少 module-design 文档；同时发现 README 任务数过期，已从 38 修正为 task_dispatcher 实际 40。

### [2026-06-15 08:22] [Cursor]
完成 TASK-137 Required Attribute Coverage Gate。新增 `AmazonListingAttributeCoverageGate`，在 API-native plan 构建后、submitter 前用 Amazon schema `required_properties` 检查 rendered payload；缺失或低置信 required 属性返回 `blocked_attribute_coverage`，不进入 PUT/VALIDATION_PREVIEW。`fabric_type` 兜底从 `AmazonListingPayloadBuilder` 专用方法迁移到 CABINET/HOME_MIRROR/OTTOMAN YAML 规则，HOME_MIRROR fallback 为 `Glass, Metal` 并带 evidence。相关发品服务测试 45 passed，全量 `.venv/bin/pytest -q` 通过，`git diff --check` 通过；报告：`docs/test-reports/2026-06-15-required-attribute-coverage-gate.md`。

### [2026-06-15 08:36] [Cursor]
完成 TASK-137 生产部署。镜像升级为 `amz-listing-management-system:2026-06-15-required-coverage`，主容器 healthy，`amz-price-inventory-scheduler` / `amz-order-sync-scheduler` / `amz-order-daily-report-scheduler` 均 running；公网 `https://amz-listing.meowy.fans` 返回 SSO 302；`list-categories` 返回 CABINET/HOME_MIRROR。生产 HOME_MIRROR strict dry-run：12 个 `skipped_existing_scope`、7 个 `blocked_variation_resolution`、6 个 `validation_preview_issues`（每 plan 15 issues），确认最新 6 条 preview `missing_required=0` 且无 `fabric_type` issue。部署时曾因当前 shell 残留本地 `.env` 导致首次重建容器注入 `DATABASE_USER=postgres`，已通过清理 shell env 后 `docker compose up -d --force-recreate` 修正；后续 deploy 前需避免在同一 shell 中 source 项目本地 `.env`。

### [2026-06-15 10:25] [Cursor]
完成 TASK-138（EPIC-AMZ-SCHEMA-DRIVEN-ATTRIBUTES P1）。`AmazonSchemaService` 新增 expanded / learned / coverage required：递归解析 Product Type Definitions 的顶层、nested、allOf/conditional required，并复用现有 `amazon_api_submissions.response_body->issues` 查询 Amazon `90220` 缺失必填反馈作为 learned required，无新增 migration。`AmazonListingAttributeCoverageGate` 优先使用 `get_coverage_required_properties()`，向后兼容旧 schema service。验证：目标测试 23 passed，发品服务回归 37 passed，全量 `.venv/bin/pytest -q` 通过；报告 `docs/test-reports/2026-06-15-schema-driven-task-138.md`。

### [2026-06-15 10:36] [Cursor]
完成 TASK-139（EPIC-AMZ-SCHEMA-DRIVEN-ATTRIBUTES P2）。`AmazonSchemaService` 新增 `get_property_names()`；`AttributePayloadRenderer` 支持 schema allowlist、`measure`、`object`/`nested_object` shape；`AmazonListingPayloadBuilder` 在返回 plan 前对全量 attributes 执行 product type schema allowlist 过滤，fail-open 保留离线 dry-run。该通用过滤用于阻断 HOME_MIRROR 的 `item_depth` / `item_type_name` / `item_width` / `item_height` / `target_audience_base` 等不适用属性外漏。验证：目标测试 30 passed，发品质量回归 44 passed，全量 `.venv/bin/pytest -q` 通过；报告 `docs/test-reports/2026-06-15-schema-driven-task-139.md`。

### [2026-06-15 10:49] [Cursor]
完成 TASK-140（EPIC-AMZ-SCHEMA-DRIVEN-ATTRIBUTES P3）。新增 `LLMAttributeExtractor` / `LLMAttributeExtraction`，`AttributeResolver` 支持 YAML `llm` source；默认不外呼，需显式注入 extractor/client。安全契约：缺证据返回 null，`enum_locked` 按 schema valid values 校验，brand/identifier/GTIN/compliance/certification/supplier_declared 等敏感字段预拒绝，置信度最高 medium；required 属性即使由 LLM 提取到也标记 `needs_manual_review` 且 blocking，避免静默 LIVE。验证：目标测试 10 passed，schema attribute pipeline 组合 41 passed，发品回归 44 passed，全量 `.venv/bin/pytest -q` 通过；报告 `docs/test-reports/2026-06-15-schema-driven-task-140.md`。

### [2026-06-15 10:58] [Cursor]
完成 TASK-141（EPIC-AMZ-SCHEMA-DRIVEN-ATTRIBUTES P4）。CABINET `dimension_strategy: item_depth_width_height` 与 `post_processors: [cabinet_attribute_shapes]` 迁入 `config/amz_listing_data_mapping/api_attribute_rules/cabinet.yaml`；`AmazonListingPayloadBuilder` 移除直接 CABINET 分支和 `normalize_cabinet_attributes` 调用，尺寸 shape 和 CABINET schema-shape normalizer 由配置驱动；新增 `attribute_post_processors.py` 作为处理器注册表。验证：目标测试 23 passed，发品回归 51 passed，schema pipeline 回归 21 passed，全量 `.venv/bin/pytest -q` 通过；报告 `docs/test-reports/2026-06-15-schema-driven-task-141.md`。

### [2026-06-15 11:03] [Cursor]
完成 EPIC-AMZ-SCHEMA-DRIVEN-ATTRIBUTES / TASK-142 生产部署。镜像 `amz-listing-management-system:2026-06-15-schema-driven-attributes` 已部署，主容器 healthy，3 个 scheduler running，公网 SSO 302，`list-categories` 正常。HOME_MIRROR strict dry-run：6 个 plan 全部 `validation_preview_passed`、issues=0、无 PUT；12 个 `skipped_existing_scope`，7 个 `blocked_variation_resolution` 后续单独处理。OTTOMAN strict smoke parent passed、2 child skipped existing。CABINET 18-SKU 回归已消除 attribute coverage block；剩余 4 个 width>42in Amazon warning 属 TASK-134 既有 business/live blocker。部署时继续使用干净 env 执行 docker compose，避免 shell 环境变量覆盖生产 `.env`。报告 `docs/test-reports/2026-06-15-schema-driven-attributes-production-deploy.md`；`/data/README.md` 与 `/data/TODO.md` 已同步。

### [2026-06-15 11:47] [Cursor]
完成 TASK-143 自动类目映射。新增 `AutoCategoryMapper` 与 CLI `auto-discover-category`，支持 `--category-code` / `--all-unmapped`，默认 dry-run；基于 Giga listing-eligible 商品采样、Amazon Catalog ASIN 搜索、`productTypes[].productType` 批量摘要投票，高置信才写入仍为空的 `supplier_categories_map` 并预缓存 schema。真实 API 验收发现 `productType` 在 Catalog `productTypes[]` 而非 `summaries[]`，已修正 `AmazonCatalogClient.batch_get_summaries()`；同时 ASIN identifiers 批量 lookup 需逗号串，否则 Amazon 只返回部分结果。验收：`10027 / Sofas` dry-run 5/5 票选 `SOFA`；小批量 no-dry-run 已写入 `10027 -> SOFA` 且 schema cached；当前 unmapped Giga categories=29。全量 `.venv/bin/pytest -q` 与 `git diff --check` 通过；报告 `docs/test-reports/2026-06-15-auto-category-mapping.md`。

### [2026-06-15 11:51] [Cursor]
完成 TASK-143 生产部署。镜像升级为 `amz-listing-management-system:2026-06-15-auto-category-mapping`，使用干净 `env -u ... docker compose` 构建并 `up -d --force-recreate`，避免 shell 环境变量覆盖生产 `.env`。生产 smoke：主容器 healthy，`amz-price-inventory-scheduler` / `amz-order-sync-scheduler` / `amz-order-daily-report-scheduler` 均 running，公网 `https://amz-listing.meowy.fans` 返回 SSO 302，`list-categories` 正常。部署后新镜像内执行 `auto-discover-category --category-code 10027`：`dry_run_selected`，Catalog votes `SOFA=5`，Written=False，Schema cached=False。`/data/README.md`、`/data/TODO.md`、`STATUS.md`、`TODO.md` 与报告已同步部署状态。
