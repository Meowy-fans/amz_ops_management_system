### [2026-06-27 09:35] [Codex]
完成 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` S5/S6/S7 engine wiring 修正：
1. `ListingPayloadEngineV2.build_read_only_plan()` 现在默认注入 `LLMAttributeExtractorV2` 到 `EvidenceResolverV2`，执行 `ConfidenceScorerV2.score_tree()`，并在 payload compose / coverage 前写回 review 状态。
2. Required LLM leaf 若评分 route 为 `ai_agent` / `human`，会标记 `review_status="pending"`、`blocking=True`、`NEEDS_REVIEW_REQUIRED_ATTRIBUTE`，使 `CoverageGateV2` 与 `ReviewAdapterV2` 消费同一 path-level pending review 契约；`auto_approved` required leaf 会标记 `review_status="auto_approved"`。
3. 新增 engine 级回归测试覆盖 `frame.color.value` 从 LLM source 解析、评分、进入 pending review、被 review adapter 提取的闭环。
4. 验证：`.venv/bin/pytest tests/unit/services/test_listing_payload_engine_v2.py tests/unit/services/test_evidence_resolver_v2.py tests/unit/services/test_confidence_scorer_v2.py tests/unit/services/test_review_adapter_v2.py tests/unit/services/test_coverage_gate_v2.py` 通过（35 passed）；`.venv/bin/ruff check src/services/listing_payload_engine_v2.py tests/unit/services/test_listing_payload_engine_v2.py` 通过。
5. 文档已同步：`STATUS.md`、`TODO.md`、`docs/epics/listing-requirement-payload-engine-v2.md`、`docs/module-design/listing-requirement-payload-engine-v2.md`。下一步仍是 S12 plan builder shadow adapter。

### [2026-06-27 10:05] [Codex]
完成 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` S12 plan builder shadow adapter 第一版：
1. 新增 `src/services/listing_payload_shadow_adapter_v2.py`，通过 `ListingPayloadEngineV2.build_read_only_plan()` 构建 V2 shadow plan，并写入 `amazon_api_submissions`，`operation="listing_payload_v2_shadow"`。
2. `ProductListingAPIPlanBuilder` 新增 `LISTING_PAYLOAD_ENGINE=shadow` 旁路 hook；默认 `v1` 不调用。shadow 成功写 `shadow_built`，异常写 `shadow_failed`，并且 adapter 异常会被 builder 捕获，不影响 V1 plan / pre-submit 结果。
3. shadow 审计 request 保存 V1 `v1_status`、`v1_attribute_names`、`v1_attributes`；response 保存 V2 attributes、required paths、coverage findings、condition traces、pending review paths 和 summary counts，为 S13 diff tooling 喂数。
4. 验证：`.venv/bin/pytest tests/unit/services/test_listing_payload_shadow_adapter_v2.py tests/unit/services/test_product_listing_api_plan_builder.py tests/unit/services/test_listing_payload_engine_v2.py` 通过（8 passed）；`.venv/bin/ruff check src/services/listing_payload_shadow_adapter_v2.py src/services/product_listing_api_plan_builder.py tests/unit/services/test_listing_payload_shadow_adapter_v2.py tests/unit/services/test_product_listing_api_plan_builder.py` 通过。
5. 文档已同步：`STATUS.md`、`TODO.md`、`docs/epics/listing-requirement-payload-engine-v2.md`、`docs/module-design/listing-requirement-payload-engine-v2.md`、`src/services/AI_SUMMARY.md`。下一步：S13 V1/V2 diff tooling。

### [2026-06-27 10:28] [Codex]
完成 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` S13 V1/V2 shadow diff tooling 第一版：
1. 新增 `src/services/listing_payload_shadow_diff_v2.py`，读取 `listing_payload_v2_shadow` 审计行并输出 V1/V2 attribute delta、V2 missing/low-confidence/pending-review paths、finding/blocking codes、condition trace count 和汇总计数。
2. `AmazonAPISubmissionRepository` 新增 `list_listing_payload_v2_shadow_submissions(product_type, sku, limit)` 查询最近 shadow 审计行。
3. CLI 注册 `report-listing-shadow-diff-v2`，支持 `--product-type/--category` 和 `--sku` 过滤；`LISTING_V2_SHADOW_DIFF_LIMIT` 控制默认 20 条。该入口只读，不调用 Amazon，不改变 V1 决策。
4. 验证：`.venv/bin/pytest tests/unit/services/test_listing_payload_shadow_diff_v2.py tests/unit/services/test_listing_payload_shadow_adapter_v2.py tests/unit/services/test_product_listing_api_plan_builder.py tests/unit/cli/test_task_dispatcher.py tests/integration/repositories/test_amazon_api_submission_repository_sql_contract.py` 通过（34 passed）；对应改动文件 ruff 通过。
5. 文档已同步：`STATUS.md`、`TODO.md`、`docs/epics/listing-requirement-payload-engine-v2.md`、`docs/module-design/listing-requirement-payload-engine-v2.md`、`src/services/AI_SUMMARY.md`。下一步：S14 multi-category regression + cutover plan。

### [2026-06-27 10:55] [Codex]
完成 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` S14 regression evaluator + cutover plan 第一版：
1. 新增 `src/services/listing_payload_v2_regression.py`，消费 S13 shadow diff summaries，按类目输出 go/no-go：live regression 类目（默认 CABINET/HOME_MIRROR/OTTOMAN）要求有 shadow 样本且无 V2 blocking；exploratory 类目（默认 CHAIR/SOFA）允许 explainable blocking codes（missing required / low confidence / pending review / unsafe default）。
2. CLI 注册 `evaluate-listing-v2-regression`，支持 `--category/--product-type` 单类目评估；`LISTING_V2_REGRESSION_CATEGORIES` 和 `LISTING_V2_REGRESSION_LIMIT` 控制默认评估范围和每类目行数。
3. 新增退役/切换计划 `docs/retirement/listing-payload-engine-v2-cutover-2026-06-27.md`。当前只定义 gate 与清单，不给旧模块加 `@retire`，不切 `LISTING_PAYLOAD_ENGINE=v2`。
4. 验证：`.venv/bin/pytest tests/unit/services/test_listing_payload_v2_regression.py tests/unit/services/test_listing_payload_shadow_diff_v2.py tests/unit/services/test_listing_payload_shadow_adapter_v2.py tests/unit/cli/test_task_dispatcher.py` 通过（29 passed）；对应改动文件 ruff 通过。
5. 文档已同步：`STATUS.md`、`TODO.md`、`docs/epics/listing-requirement-payload-engine-v2.md`、`docs/module-design/listing-requirement-payload-engine-v2.md`、`src/services/AI_SUMMARY.md`。下一步：采集 CABINET/HOME_MIRROR/OTTOMAN/CHAIR/SOFA 真实 shadow/strict-preview evidence，再实现 `LISTING_PAYLOAD_ENGINE=v2` replacement/canary。

### [2026-06-27 11:12] [Codex]
接通 `generate-listing-api --engine shadow` evidence collection 入口：
1. `main.py` 的 `--engine` choices 扩展为 `v1|v2|shadow`。
2. `handle_generate_listing_api()` 接收 engine 参数并设置 `ProductListingService.listing_payload_engine_mode`，从而触发 `ProductListingAPIPlanBuilder` 已有 V2 shadow audit hook。
3. 安全边界：默认仍为 `v1`；发品入口的 `--engine v2` 返回 `listing_payload_engine_v2_not_enabled`；`--engine shadow --no-dry-run` 返回 `shadow_engine_requires_dry_run`，防止未 canary 前误切 LIVE。
4. 验证：`.venv/bin/pytest tests/unit/cli/test_listing_handlers.py tests/unit/cli/test_task_dispatcher.py tests/integration/cli/test_main_non_interactive_entrypoint.py tests/unit/services/test_product_listing_api_plan_builder.py tests/unit/services/test_listing_payload_shadow_adapter_v2.py tests/unit/services/test_listing_payload_v2_regression.py` 通过（45 passed）；对应改动文件 ruff 通过。
5. 文档已同步：`STATUS.md`、`TODO.md`、`docs/module-design/listing-requirement-payload-engine-v2.md`、`docs/retirement/listing-payload-engine-v2-cutover-2026-06-27.md`。下一步可以用 `generate-listing-api --engine shadow --strict-validation --sku ...` 采集真实多类目 evidence。

### [2026-06-27 10:24] [Codex]
推进 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` CABINET shadow evidence，并完成首条 CABINET latest-row go：
1. 通过临时 Docker 容器挂载当前工作区源码、复用生产 env 与 Docker `proxy` 网络执行 `generate-listing-api --engine shadow --strict-validation --category CABINET --sku meow251115FC0ie`。V1 仍为 dry-run authoritative，结果 `skipped_existing`，未 PUT；V2 只写 `listing_payload_v2_shadow` 审计行。
2. 修复 shadow diff SKU 过滤大小写不兼容；`report-listing-shadow-diff-v2` 现在打印 missing / pending / low-confidence path 明细。
3. 修复 V2 通用 object payload 生成缺口：`PayloadComposerV2` 支持 `array_object` scalar/list scalar parent value 渲染；`EvidenceResolverV2` 支持 scalar-list 与 list-of-dict child 继承，并在规则缺失时 fallback 到 V2 candidate attributes。
4. `ListingPayloadEngineV2` candidate builder 补齐 V1 已有 deterministic 字段：color/material、dimensions/weight、part/model、offer、fulfillment；`CoverageGateV2` 新增规则驱动 `coverage_ignore_required`，CABINET 忽略 V1 也不提交的 `merchant_shipping_group` / `merchant_suggested_asin`。
5. CABINET `meow251115FC0ie` 最新 shadow submission `114150`：V2 attrs=31、missing=0、pending=0、blocking=0；`LISTING_V2_REGRESSION_LIMIT=1 evaluate-listing-v2-regression --category CABINET` 返回 `go`。报告：`docs/test-reports/2026-06-27-listing-payload-v2-cabinet-shadow.md`。
6. 验证：V2/CLI/repo targeted suite 124 passed，改动文件 ruff 通过。下一步：采集 HOME_MIRROR / OTTOMAN / CHAIR / SOFA evidence，并让 regression evaluator 默认按 SKU 最新 shadow 行评估，避免旧 shadow 行污染 go/no-go。

### [2026-06-27 17:19] [Codex]
继续推进 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` 多类目 shadow regression：
1. `ListingPayloadV2Regression` 已按 SKU 最新 shadow 行评估，避免历史失败 row 污染 go/no-go；shadow diff SKU 过滤保持大小写不敏感。
2. HOME_MIRROR `meow251108CqW5i` 最新 shadow submission `115998`：V2 attrs=26、missing=0、pending=0、blocking=0，regression `go`。
3. OTTOMAN 全家族 `meow2511088jSUW` / `meow260518LZZCw` 最新 shadow submissions `116003` / `116002`：两条均 missing=0、pending=0、blocking=0；V1 parent `PARENT-FE6D7B7BD7A8` strict preview passed，children skipped existing；regression `go`。
4. CHAIR `meow2511081Gqqd` shadow submission `116007` 为 exploratory `go`，阻断项属于 explainable missing rule / pending review。
5. SOFA `meow251108Bg4d4` shadow submission `116008` 为 exploratory `go`；随后补 `sofa.yaml` 的 `coverage_ignore_required` 与结构化 `seat.depth/height/interior_width/fill_material/material_type` child rules。直接 V2 read-only plan after fix：missing=0，仅 `seating_capacity.value` / `sofa_type.value` pending review。
6. 新增综合报告 `docs/test-reports/2026-06-27-listing-payload-v2-shadow-regression.md`；更新 `STATUS.md`、`TODO.md`、Epic、module design、cutover plan。验证：V2/CLI/repo targeted suite 127 passed，ruff 通过，`git diff --check` 通过。下一步：实现 `LISTING_PAYLOAD_ENGINE=v2` authoritative dry-run/canary replacement path。

### [2026-06-27 17:36] [Codex]
完成 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` authoritative dry-run canary path 第一版：
1. `generate-listing-api --engine v2` 现在允许 dry-run / strict-validation；LIVE `--no-dry-run` 在 CLI 与 `ProductListingService.generate_listings_via_api()` 双层返回 `v2_engine_requires_dry_run`。
2. `ProductListingAPIPlanBuilder` V2 模式使用 `ListingPayloadEngineV2.build_read_only_plan_from_draft()` 生成 authoritative attributes / V2 coverage，不再走 V1 resolver/renderer/coverage；draft-based 入口保留上游 commercial、image、variation 决策。
3. `ListingPayloadEngineV2` 在 required tree composition 后合并 schema-allowed deterministic candidate attributes，保留 `main_product_image_locator`、offer、fulfillment、variation attributes 等非 required 但 submitter/业务必需字段；`purchasable_offer` 与 V1 一样带 `marketplace_id`。
4. V2 coverage review-only block 会通过 `ReviewAdapterV2.persist_pending_paths()` 写 path-level pending review。
5. 真实 CABINET canary：`generate-listing-api --category CABINET --sku meow251115FC0ie --strict-validation --engine v2` 已进入 V2 authoritative submitter path，最终因 Amazon 已存在返回 `skipped_existing`，无 PUT；此前暴露的 `MISSING_MAIN_IMAGE` 已修复。
6. 验证：V2/CLI/service/builder targeted suite 154 passed，受影响文件 ruff 通过，`git diff --check` 通过。下一步：对 HOME_MIRROR / OTTOMAN 继续跑 V2 strict-validation parity，并选择至少 1 个非 existing SKU 触发 Amazon `VALIDATION_PREVIEW`。

### [2026-06-27 17:46] [Codex]
继续推进 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` authoritative strict-preview parity：
1. HOME_MIRROR `meow251108CqW5i` 走 `generate-listing-api --engine v2 --strict-validation`，进入 V2 authoritative submitter path 后返回 `skipped_existing`，无 PUT。
2. OTTOMAN family `meow2511088jSUW` / `meow260518LZZCw` 初次 V2 authoritative run 被 parent coverage 阻断，根因是 `RequirementTreeBuilderV2` 仍继承旧 cache expanded `required_properties` / 条件 required，对 `parentage_level=parent` 误要求 `child_parent_sku_relationship`。
3. 修复：V2 required 起点改为 raw schema `required` + 动态条件分支，不再使用旧 expanded `required_properties`；新增通用 Amazon variation parent 语义，`parentage_level=parent` 时不把 `child_parent_sku_relationship` 作为 parent required，child 场景仍保留。
4. 修复后复跑 OTTOMAN V2 strict-validation：生成 3 个 plan，新 parent `PARENT-818700D0BEB9` Amazon `VALIDATION_PREVIEW` passed（0 issues），两个 child `skipped_existing`，无 PUT。
5. 新增报告 `docs/test-reports/2026-06-27-listing-payload-v2-authoritative-canary.md`；更新 STATUS/TODO/Epic/module design/cutover plan。验证：V2/CLI/service/builder/tree targeted suite 167 passed，ruff 通过，`git diff --check` 通过。下一步：做 V2 path-level review resume smoke，再评估 LIVE canary 准入。

### [2026-06-27 17:50] [Codex]
为后续 Agent 接手补充 V2 Epic handoff 文档：
1. `docs/test-reports/2026-06-27-listing-payload-v2-authoritative-canary.md` 新增 `Next Agent Handoff`，明确下一步做 V2 path-level review resume smoke，推荐从 SOFA `meow251108Bg4d4` 开始，并列出 `review-pending-attributes --engine v2` / `submit-reviewed-plans --engine v2` 命令和验收标准。
2. `docs/retirement/listing-payload-engine-v2-cutover-2026-06-27.md` 的执行策略已更新为当前真实状态：shadow 用于 V1+V2 audit，V2 只允许 dry-run/strict-validation authoritative canary，LIVE 仍双层阻断。
3. `STATUS.md` 与 Epic 文档已补接手说明：下一步验收为 pending row 落库、审核推进、approved override replay 后同一路径不再阻断；禁止启用 `--engine v2 --no-dry-run` 或提前给 V1 模块加 `@retire`。

### [2026-06-27 18:20] [Cursor]
完成 `EPIC-AMZ-LISTING-REQUIREMENT-PAYLOAD-V2` V2 path-level review resume smoke：
1. 生产 DB 迁移：执行 alembic `011_amz_listing_pending_review_v2` + `012_amz_listing_learned_required_paths_v2`；因 revision id 超过 32 字符，先将 `alembic_version.version_num` 扩至 `VARCHAR(64)`。
2. 新增 `ReviewAdapterV2.approve_human_pending_paths()` 与 CLI `--approve-human`（`review-pending-attributes --engine v2`）；V2 authoritative plan builder 加载 completed review overrides；`EvidenceResolverV2` 子路径 override 优先于 parent inheritance；upsert 保留 `completed` review 状态。
3. SOFA `meow251108Bg4d4` smoke：`generate-listing-api --engine v2` -> 2 条 pending row；`--approve-human` -> completed；`submit-reviewed-plans --engine v2` -> `dry_run_preview`；重建 -> `skipped_existing`，review rows 保持 completed。
4. 验证：V2 targeted suite 176 passed；ruff / `git diff --check` 通过。报告：`docs/test-reports/2026-06-27-listing-payload-v2-review-resume-smoke.md`。下一步：LIVE canary 准入评估、`ValidationPreviewV2.compare()`、`operation_handlers_v2.py` 拆分。

### [2026-06-27 10:52] [Cursor]
5. Validation compare gate：`build_v2_payload_plan_for_sku()` 对齐 authoritative path；`ListingPayloadEngineV2` 应用 `post_processors` + universal identifier V1 shape parity；`measure_array` 修复 OTTOMAN `seat.height`；compare 尊重 `coverage_ignore_required`。
6. 生产证据：`evaluate-listing-v2-validation-compare` 在挂载 workspace + `.env.amazon-sp-api` 下 `status=go`（CABINET/HOME_MIRROR/OTTOMAN 3/3）。报告：`docs/test-reports/2026-06-27-listing-payload-v2-validation-compare.md`。
7. LIVE canary 仍未开放：`--engine v2 --no-dry-run` 阻断；需更多 non-existing SKU strict-preview evidence。

### [2026-06-27 11:12] [Cursor]
8. 构建并部署生产镜像 `amz-listing-management-system:2026-06-27-listing-payload-v2`；容器内 `evaluate-listing-v2-validation-compare` go 3/3（无 workspace 挂载）。
9. OTTOMAN variation 新 parent `PARENT-B44F29098D69` 在烘焙镜像上 `validation_preview_passed`；CABINET 候选 SKU 大多已上 Amazon（`skipped_existing_scope`）。
10. LIVE canary 准入评估结论：**NOT READY**；报告 `docs/test-reports/2026-06-27-listing-payload-v2-live-canary-readiness.md`。

### [2026-06-27 19:58] [Cursor]
1. Epic 内 SOFA 3 SKU 阻断根因：`product_description` 无 fallback、`Seats` 未映射、`sofa_type` LLM review、`seat.fill_material` enum token 错误。
2. 修复：通用 `attribute_transform_helpers`（`text_join`/`enum_scan`/integer token parse）+ universal preset / `sofa.yaml` 规则；无 SKU hard code。
3. 构建并部署 `amz-listing-management-system:2026-06-27-sofa-v2-coverage-rules`；生产验收：healthy + validation compare go 3/3 + SOFA `meow25110865jrz` `validation_preview_passed`。
4. 报告：`docs/test-reports/2026-06-27-sofa-v2-coverage-rules-production-deploy.md`；已更新 `/data/README.md`、`/data/TODO.md`。

### [2026-06-27] [Cursor]
基于代码审查修订 `docs/epics/listing-rule-authoring-v2.md`：扩展问题域至 measure/array_object 嵌套与 product-type 级配置；新增 §1.3 三层审核分离、§1.4 candidate_attributes 协调、§1.5 loader 浅合并约束；S1 要求复用 RequirementTreeBuilderV2、structural parent 不写 sources；S6 全量 attribute 块迁移；验收增加 dimension_strategy 与 leaf TODO KPI；工期调整为 20-35 人天；§6 关键决策由 open 改为 resolved。

### [2026-06-27 20:55] [Cursor]
CHAIR Quick Validation 实验完成 — 手工补丁 `chair.yaml`（`dimension_strategy`、CHAIR 形 `seat`/`frame`/`item_depth_width_height` children、measure 子节点、safe defaults）。离线 V2：主 SKU missing 9→0；CHAIR 18 SKU 池 zero_missing 18/18（9 个仍有 findings）。Amazon `validate-listing-v2` 主 SKU：`validation_preview_issues`，90220 缺 `frame.material`/`seat.depth`（非 analyze 静态树可见项）。`generate-listing-api --engine v2 --strict-validation`：`needs_review`。结论：Epic 假设成立（瓶颈在 YAML），但不可盲拷 SOFA seat；S7 preview passed 未达。报告：`docs/test-reports/2026-06-27-chair-quick-validation.md`；baseline：`chair.yaml.baseline`。

### [2026-06-27 21:26] [Cursor]
EPIC Rule Authoring S1 落地：`rule_skeleton_generator_v2` + CLI `generate-rule-skeleton-v2`；`optional_rule_children_enricher_v2` 将 YAML 中 RequirementTree 未覆盖的可选子节点（如 90220 preview 的 frame.material/seat.depth）并入 payload。CHAIR 规则补全后 `validate-listing-v2 meow2511081Gqqd` → `validation_preview_passed`（amazon_issues=0）。单测：skeleton 2 + enricher 1 passed。
