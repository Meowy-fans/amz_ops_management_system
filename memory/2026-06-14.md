### [2026-06-14 05:17] [Codex]
完成 TASK-128：SKU 级发品范围控制与审计摘要。`generate-listing-api` 支持 `--sku`（可重复/逗号分隔）、`--sku-file`、`--only-not-on-amazon`，可精确复跑 CABINET 18-SKU 验收集；新增 `ListingScope` / `ProductListingScopeFilter`，候选集按本地 eligible、category 映射、Amazon 只读存在性检查过滤，审计状态包含 `blocked_scope_filter` 和 `skipped_existing_scope`，非 404 Amazon 查询错误 fail closed。`ProductListingService.generate_listings_via_api()` 返回 `audit.scope` 与 `audit.result_status_counts`，CLI 打印状态摘要。验证：scope/service/CLI 目标测试 46 passed，全量 `.venv/bin/pytest -q` 通过，676 tests collected。

### [2026-06-14 05:24] [Codex]
完成 TASK-129 CABINET 18-SKU strict dry-run 验收执行，结论为技术链路通过但业务验收未通过。生产容器临时目录 `/tmp/codex-amz-task129` 运行当前代码：18 个目标 SKU 未扩散到全 CABINET；9 个 `skipped_existing_scope`、3 个 `blocked_commercial_gate`、2 个 `blocked_variation_resolution`、6 个 `validation_preview_issues`，无 PUT。Amazon preview 暴露 P0：`door` required 缺失、`variation_theme=Color` 非法、`item_weight`/`item_depth_width_height` unit 缺失、`item_type_name`/`target_audience_base` 对 CABINET 不适用。报告写入 `docs/test-reports/2026-06-14-cabinet-18-strict-dry-run.md`，TODO 新增 TASK-132。

### [2026-06-14 05:35] [Codex]
完成 TASK-132 CABINET strict validation P0 属性修复并复跑 Amazon preview。修复内容：CABINET `door` 改为 schema nested `style` shape；`variation_theme` 增加 fallback（`Color`→`COLOR`，`Color/Size`→`COLOR/ITEM_WIDTH`）；CABINET `size_name` variation attribute 渲染为 `item_width` measure；移除 CABINET 不适用 `item_type_name`/`target_audience_base`；Giga combo SKU 主尺寸为 Not Applicable 时从 `comboInfo` 汇总尺寸/重量。复跑 18-SKU strict dry-run：`validation_preview_passed=4`、`validation_preview_issues=0`，无 PUT。剩余 blocker：2 个 dimension range quality block、2 个 duplicate variation block、3 个 quantity cap block。全量 `.venv/bin/pytest -q` 通过，679 tests collected。

### [2026-06-14 05:58] [Codex]
完成 TASK-133 CABINET 剩余 blocker 收敛：Commercial Gate 库存超限改为 clamp+审计（source_publish_quantity/publish_quantity），API payload 使用截断发布库存；新建 variation parent quantity 固定 0；CABINET quality gate 尺寸规则迁移到 config/listing_gates/quality_gate.yaml；补 Size→ITEM_WIDTH、Color/Size→COLOR/ITEM_WIDTH 的 item_width 渲染和缺 item_width fail-closed；Variation resolver 跳过 Not Applicable 并从 24inch/48" 标题提取尺寸；Giga combo 只有重量时也从 comboInfo 补空间尺寸；country_of_origin 增补 Malaysia→MY。最终 18-SKU strict dry-run（生产容器临时目录 /tmp/codex-amz-task129，无 PUT）：9 skipped_existing_scope、3 blocked_variation_resolution、4 validation_preview_passed、4 validation_preview_issues（均为 Amazon 100335 WARNING：CABINET width 52.76/53.54in > max 42in）。全量 .venv/bin/pytest -q 通过。剩余 LIVE 前业务决策：48in vanity product type/尺寸字段策略，以及 3 个重复 variation signature 的人工处理。
