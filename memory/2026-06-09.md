
### [2026-06-09 02:38] [Codex]
完成 confirmation listing issue 推荐属性自动补全独立流程：新增 `sync-confirmation-listing-issues` 从价格/库存 delayed_confirmation 审计中按 SKU 最新 getListingsItem 响应同步 issues 到 `amazon_listing_issues`（source=`price_inventory_confirmation`），新增/增强 `repair-listing-issues` 仅对高置信 `18448 recommended_uses_for_product` 生成/提交安全 PATCH，新增 `confirm-listing-issue-repairs` 30 分钟后确认原 issue 是否消失并写入 `repair_confirmed`/`repair_failed` 动作；修复动作记录 target_attribute/target_value/confidence/evidence，live PATCH 只有 ACCEPTED 且无 issues 才进入 submitted。更新 `docs/listing-issue-monitoring.md`、`STATUS.md`、`TODO.md`。验证：目标 32 tests passed，全量 `.venv/bin/pytest -q` 通过，当前测试数 620；`.venv/bin/python -m compileall` 通过。

### [2026-06-09 03:29] [Codex]
将 confirmation listing issue 推荐属性自动补全流程部署到生产并完成真实闭环验证：生产重建 `amz-listing-management-system:2026-06-08` 主服务和 scheduler，主容器 healthy；缓存 CABINET Product Type schema 后，`sync-confirmation-listing-issues` dry-run 从 delayed_confirmation 审计同步 169 issues，其中 95 个 `18448 recommended_uses_for_product` 生成安全 PATCH、74 个非白名单 issue 保持 manual。执行 `repair-listing-issues --no-dry-run` 后 95 个 PATCH 均 submitted；等待 30 分钟后执行 `confirm-listing-issue-repairs`，95/95 `repair_confirmed`。随后发现旧 delayed_confirmation 快照可能重开已修复 issue，已补 `has_confirmed_repair_after` 保护并重部署；再次运行 `sync-confirmation-listing-issues` 只同步 74 个 manual issue，`recommended_uses_for_product` open issue 为 0。验证：全量 `.venv/bin/pytest -q` 通过，当前测试数 622。
