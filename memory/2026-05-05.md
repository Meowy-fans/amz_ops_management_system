### [2026-05-05 10:17] [Codex]
加强集成测试 Phase 7：新增 `tests/integration/services/test_product_listing_generation_flow.py`，用真实 `ProductListingService`、`VariationHelper`、`DataMappingHelper`、`ExcelGenerator` 覆盖待发 SKU 过滤、变体识别、字段映射、Excel 文件生成、发品日志构造与事务提交协作链路；外部 DB/LLM 边界用内存 fake 隔离。验证：`python3 -m pytest` 为 128 passed；`.venv/bin/pytest --cov=src --cov=infrastructure --cov-report=term-missing --cov-fail-under=35` 通过，总覆盖率 60.79%。

### [2026-05-05 11:17] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_product_listing_repository_sql_contract.py`，用记录型 session 固定 `ProductListingRepository` 的待发品筛选 SQL、变体 latest_records/JSONB 清洗、品类映射大小写 join、空输入不触库和异常重抛契约。验证：`.venv/bin/pytest` 为 134 passed；覆盖率命令通过，总覆盖率 61.02%，`ProductListingRepository` 覆盖率 88%。

### [2026-05-05 11:24] [Codex]
继续加强集成测试：新增 `tests/integration/services/test_giga_sync_flow.py`，用 fake Giga API / repository / DB / reporter 覆盖 `GigaSyncService` 的 SKU 分页获取、详情批处理、repository 保存、事务 commit/rollback、统计和 reporter 输出协作链路。验证：`.venv/bin/pytest` 为 137 passed；覆盖率命令通过，总覆盖率 61.28%，`GigaSyncService` 覆盖率 88%。

### [2026-05-05 11:29] [Codex]
继续加强集成测试：新增 `tests/integration/services/test_giga_price_sync_flow.py`，用 fake Giga API / price repository / DB / reporter 覆盖 `GigaPriceSyncService` 的价格批处理、API 重试、限流等待、repository 保存、事务 commit/rollback 和统计输出。验证：`.venv/bin/pytest` 为 140 passed；覆盖率命令通过，总覆盖率 62.74%，`GigaPriceSyncService` 覆盖率 98%。

### [2026-05-05 11:34] [Codex]
继续加强集成测试：新增 `tests/integration/services/test_giga_inventory_sync_flow.py`，用 fake inventory repository / reporter 和真实 `sync_all_inventory()` 调度逻辑覆盖库存批次切分、成功/失败批次聚合、限流等待、仓库异常兜底和最终统计输出。验证：`.venv/bin/pytest` 为 142 passed；覆盖率命令通过，总覆盖率 62.90%，`GigaInventorySyncService` 覆盖率 87%。

### [2026-05-05 11:45] [Codex]
继续加强集成测试：新增 `tests/integration/services/test_inventory_price_update_file_flow.py`，覆盖库存价格更新文件生成路径中的 SKU/价格/库存整合、TSV 表头顺序、空价格/空库存兜底和 reporter 输出。验证：`.venv/bin/pytest` 为 143 passed；覆盖率命令通过，总覆盖率 63.59%，`InventoryPriceUpdaterService` 覆盖率 96%。

### [2026-05-05 11:52] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_listing_data_repository_sql_contract.py`，固定 `ListingDataRepository` 的可更新 SKU 映射、价格/库存批量查询、空输入短路和数据库异常兜底 SQL 契约。验证：`.venv/bin/pytest` 为 149 passed；覆盖率命令通过，总覆盖率 64.31%，`ListingDataRepository` 覆盖率 100%。

### [2026-05-05 11:56] [Codex]
继续加强集成测试：新增 `tests/integration/cli/test_main_non_interactive_entrypoint.py`，覆盖 `main.py --task` 非交互入口的参数传递、DB session 生命周期、成功退出码和未知任务退出码。验证：`.venv/bin/pytest` 为 151 passed；覆盖率命令通过，总覆盖率 64.31%（当前覆盖率配置统计 `src` 和 `infrastructure`，不计入根目录 `main.py`）。

### [2026-05-05 12:25] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_pricing_repository_sql_contract.py`，固定 `PricingRepository` 的 SKU 列表查询、成本查询价格选择、非 USD/无价跳过、空 upsert、COPY upsert 和异常重抛契约。验证：`.venv/bin/pytest` 为 158 passed；覆盖率命令通过，总覆盖率 65.61%，`PricingRepository` 覆盖率 100%。

### [2026-05-05 14:19] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_giga_product_price_repository_sql_contract.py`，固定 `GigaProductPriceRepository` 的 SKU 查询、base/tier 临时表 COPY 写入、统计查询、批量基础价格失败和 tier 写入失败兜底契约。验证：`.venv/bin/pytest` 为 171 passed；覆盖率命令通过，总覆盖率 67.07%，`GigaProductPriceRepository` 覆盖率 98%。

### [2026-05-05 14:27] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_giga_product_inventory_repository_sql_contract.py`，固定 `GigaProductInventoryRepository` 的 SKU 查询、库存解析、临时表 COPY upsert、commit/rollback 和统计查询契约。验证：`.venv/bin/pytest` 为 181 passed；覆盖率命令通过，总覆盖率 68.29%，`GigaProductInventoryRepository` 覆盖率 99%。

### [2026-05-05 14:30] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_giga_product_sync_repository_sql_contract.py`，固定 `GigaProductSyncRepository` 的 product upsert、batch upsert、按 SKU 查询、统计和 SKU 列表 SQL 契约。验证：`.venv/bin/pytest` 为 190 passed；覆盖率命令通过，总覆盖率 69.18%，`GigaProductSyncRepository` 覆盖率 100%。

### [2026-05-05 14:34] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_sku_mapping_repository_sql_contract.py`，固定 `SkuMappingRepository` 的供应商 SKU 映射查询、LLM SKU 查询、未映射过滤、批量插入和统计 SQL 契约。验证：`.venv/bin/pytest` 为 200 passed；覆盖率命令通过，总覆盖率 70.01%，`SkuMappingRepository` 覆盖率 100%。

### [2026-05-05 14:37] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_llm_product_detail_repository_sql_contract.py`，固定 `LLMProductDetailRepository` 的未处理 SKU 查询、raw_data 字典/JSON 读取、批量保存 commit/rollback 和统计 SQL 契约。验证：`.venv/bin/pytest` 为 210 passed；覆盖率命令通过，总覆盖率 70.95%，`LLMProductDetailRepository` 覆盖率 98%。

### [2026-05-05 14:40] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_amz_listing_log_repository_sql_contract.py`，固定 `AmzListingLogRepository` 的 family 查询、parent 查询、变体属性 JSON 解析、批量日志 upsert 和状态更新 SQL 契约。验证：`.venv/bin/pytest` 为 220 passed；覆盖率命令通过，总覆盖率 72.19%，`AmzListingLogRepository` 覆盖率 100%。

### [2026-05-05 14:46] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_amz_template_repository_sql_contract.py`，固定 `AmzTemplateRepository` 的模板保存 JSON 序列化/commit/rollback、按品类查询 JSON 解析和默认值、最新字段定义读取、字段定义更新不提交事务、优先主题列表查询契约。验证：`.venv/bin/pytest` 为 235 passed；覆盖率命令通过，总覆盖率 73.91%，`AmzTemplateRepository` 覆盖率 99%。

### [2026-05-05 14:49] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_category_repository_sql_contract.py`，固定 `CategoryRepository` 的 SKU 到标准品类映射、供应商品类发现、未映射统计、有效 Amazon 品类集合、批量插入/更新 commit/rollback SQL 契约。验证：`.venv/bin/pytest` 为 251 passed；覆盖率命令通过，总覆盖率 75.56%，`CategoryRepository` 覆盖率 100%。

### [2026-05-05 14:52] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_amz_full_list_report_repository_sql_contract.py`，固定 `AmzFullListReportRepository` 的空 DataFrame 短路、DataFrame 临时表写入、upsert/drop、失败清理和统计查询 SQL 契约。验证：`.venv/bin/pytest` 为 257 passed；覆盖率命令通过，总覆盖率 76.05%，`AmzFullListReportRepository` 覆盖率 100%。

### [2026-05-05 14:55] [Codex]
继续加强集成测试：新增 `tests/integration/repositories/test_product_data_repository_sql_contract.py`，固定 `ProductDataRepository` 的完整产品数据 join 查询、缺失返回空字典和数据库异常重抛契约。验证：`.venv/bin/pytest` 为 260 passed；覆盖率命令通过，总覆盖率 76.36%，`ProductDataRepository` 覆盖率 100%。

### [2026-05-05 14:58] [Codex]
继续加强测试覆盖：新增 `tests/unit/services/test_product_listing_config.py`，固定发品配置默认路径发现、显式 `category_details` 加载、缺失文件和坏 JSON 兜底契约。验证：`.venv/bin/pytest` 为 268 passed；覆盖率命令通过，总覆盖率 76.83%，`product_listing_config.py` 覆盖率 100%。

### [2026-05-05 15:01] [Codex]
继续加强测试覆盖：新增 `tests/unit/utils/test_pricing_config.py`，固定 `PricingConfigLoader` 的 YAML 加载缓存、fallback、品类覆盖、缺失文件和坏 YAML 异常契约。验证：`.venv/bin/pytest` 为 274 passed；覆盖率命令通过，总覆盖率 77.30%，`pricing_config.py` 覆盖率 100%。

### [2026-05-05 15:04] [Codex]
继续加强测试覆盖：新增 `tests/unit/utils/test_data_cleaner.py`，固定 `DataCleaner` 的递归低优先级字段清理、HTML/图片 URL 清理、智能截断三层降级和简单截断契约。验证：`.venv/bin/pytest` 为 285 passed；覆盖率命令通过，总覆盖率 78.69%，`data_cleaner.py` 覆盖率 100%。

### [2026-05-05 15:08] [Codex]
继续加强测试覆盖：扩展 `tests/unit/services/test_product_detail_generation_service.py`，固定 `ProductDetailGenerationService` 的单 SKU 生成、缺原始数据/缺 prompt、LLM 重试/最终失败、批处理保存统计契约。验证：`.venv/bin/pytest` 为 292 passed；覆盖率命令通过，总覆盖率 79.72%，`ProductDetailGenerationService` 覆盖率 97%。

### [2026-05-05 15:11] [Codex]
继续加强测试覆盖：新增 `tests/unit/utils/test_prompt_manager.py`，固定 `PromptManager` 的 prompt YAML 加载、缺文件/坏 YAML 容错、缺失 prompt 和 reload 契约。验证：`.venv/bin/pytest` 为 298 passed；覆盖率命令通过，总覆盖率 79.98%，`prompt_manager.py` 覆盖率 100%。

### [2026-05-05 15:14] [Codex]
继续加强测试覆盖：新增 `tests/unit/infrastructure/test_validators.py`，固定 `validate_file_path` 的文件路径校验、目录/缺失/格式拒绝和 `AppException`/`ValidationException` code 契约。验证：`.venv/bin/pytest` 为 307 passed；覆盖率命令通过，总覆盖率 80.19%，`validators.py` 与 `exceptions.py` 覆盖率 100%。

### [2026-05-05 15:17] [Codex]
继续加强测试覆盖：新增 `tests/unit/infrastructure/llm/test_direct_llm_service.py`，固定 `DirectLLMService` 的 provider 路由、默认模型、fallback、不可用 provider 和健康检查契约。验证：`.venv/bin/pytest` 为 317 passed；覆盖率命令通过，总覆盖率 81.06%，`DirectLLMService` 覆盖率 94%。

### [2026-05-05 15:21] [Codex]
继续加强测试覆盖：新增 `tests/unit/infrastructure/llm/test_llm_api_clients.py`，固定 DeepSeek/Qwen 客户端初始化、文本/JSON 解析、空内容、API 错误和异常重抛契约；全程 monkeypatch fake 响应，不访问外部网络。验证：`.venv/bin/pytest` 为 329 passed；覆盖率命令通过，总覆盖率 81.91%，DeepSeek/Qwen 客户端覆盖率 100%。

### [2026-05-05 15:25] [Codex]
继续加强测试覆盖：新增 `tests/unit/infrastructure/giga/test_giga_clients.py`，固定 Giga 配置、token 缓存/刷新、API 成功、401 刷新、业务错误、超时和可重试异常契约；全程 monkeypatch fake response，不访问外部网络。验证：`.venv/bin/pytest` 为 344 passed；覆盖率命令通过，总覆盖率 83.79%，Giga config/token/API client 覆盖率 100%/90%/93%。

### [2026-05-05 15:30] [Codex]
继续缓解部署规范偏移：新增 `deploy/production/` 生产部署模板，按 `/data/docker-compose/amz-listing-management-system/`、共享 PostgreSQL `postgres:5432`、无宿主机端口、`.env` 敏感配置和 `/data/volumes/amz-listing-management-system/` 持久化路径固化部署契约，并更新 `docs/architecture.md` 部署拓扑。验证：临时 `.env` 下 `docker compose -f deploy/production/docker-compose.yml config` 通过；`.venv/bin/pytest` 为 344 passed。

### [2026-05-05 15:36] [Codex]
继续加强测试覆盖：新增 `tests/unit/infrastructure/llm/test_autogen_llm_service.py`，固定 `AutoGenLLMService` 的 payload、session、fallback、健康检查、final message 解析和上游错误契约。验证：`.venv/bin/pytest` 为 356 passed；覆盖率命令通过，总覆盖率 85.84%，`AutoGenLLMService` 覆盖率 98%。

### [2026-05-05 15:41] [Codex]
继续加强测试覆盖：新增 `tests/unit/cli/test_operation_handlers.py`，固定运营 CLI handler 的商品同步确认、报告导入、状态更新、AI 详情、价格/库存同步、售价更新和更新文件生成契约。验证：`.venv/bin/pytest` 为 371 passed；覆盖率命令通过，总覆盖率 87.93%，`operation_handlers.py` 覆盖率 100%。

### [2026-05-05 15:44] [Codex]
继续加强测试覆盖：扩展 `tests/unit/cli/test_query_handlers.py`，固定查询 CLI handler 的统计总览、待发品统计、最近发品、品类列表成功/空结果/异常契约。验证：`.venv/bin/pytest` 为 379 passed；覆盖率命令通过，总覆盖率 89.72%，`query_handlers.py` 覆盖率 100%。

### [2026-05-05 15:51] [Codex]
继续加强测试覆盖：扩展 `tests/unit/cli/test_category_handlers.py`，固定品类 CLI handler 的模板更新/矫正、Giga 品类同步确认与导出、CSV 映射更新和新品类导出契约。验证：`tests/unit/cli/test_category_handlers.py` 为 22 passed；覆盖率命令通过，`.venv/bin/pytest --cov=src --cov=infrastructure --cov-report=term-missing --cov-fail-under=35` 为 397 passed，总覆盖率 91.48%，`category_handlers.py` 覆盖率 100%。

### [2026-05-05 15:54] [Codex]
继续加强测试覆盖：扩展 `tests/unit/cli/test_listing_handlers.py`，固定发品 CLI handler 的交互返回/选择、成功/失败结果和系统异常契约。验证：`.venv/bin/pytest` 为 401 passed；覆盖率命令通过，总覆盖率 91.81%，`listing_handlers.py` 覆盖率 100%。

### [2026-05-05 15:57] [Codex]
继续加强测试覆盖：扩展 `tests/unit/cli/test_menu.py` 与 `tests/unit/cli/test_task_dispatcher.py`，固定 CLI 菜单无效选项、键盘中断、异常兜底和发品任务 CLI 分发契约。验证：`.venv/bin/pytest` 为 405 passed；覆盖率命令通过，总覆盖率 92.12%，`menu.py` 与 `task_dispatcher.py` 覆盖率 100%。

### [2026-05-05 16:01] [Codex]
继续加强测试覆盖：扩展 `tests/unit/services/test_template_parser_helpers.py`，固定模板解析器工作簿打开失败、缺工作表、缺表头、无 Valid Values、异常兜底和变体主题 fallback 契约。验证：`.venv/bin/pytest` 为 414 passed；覆盖率命令通过，总覆盖率 92.85%，`amz_template_parser.py` 覆盖率 96%。

### [2026-05-05 16:03] [Codex]
继续加强测试覆盖：扩展 `tests/unit/utils/test_data_mapping_helper.py`，固定数据映射 helper 的配置加载/失败、空输入、LLM 增强成功/失败和 wrapper 委托契约。验证：`.venv/bin/pytest` 为 420 passed；覆盖率命令通过，总覆盖率 93.27%，`data_mapping_helper.py` 覆盖率 85%。

### [2026-05-05 16:09] [Codex]
继续加强测试覆盖：扩展 `tests/unit/utils/test_excel_generator.py`，固定 Excel 生成器默认路径发现、空输入、保存异常、重复表头解析和缺少硬编码字段告警契约。验证：`.venv/bin/pytest` 为 425 passed；覆盖率命令通过，总覆盖率 93.72%，`excel_generator.py` 覆盖率 98%。

### [2026-05-05 16:12] [Codex]
继续加强测试覆盖：扩展 `tests/unit/services/test_amz_template_management_service.py`，固定模板管理服务的模板文件缺失、保存失败、报错解析失败、无模板记录、无需更新、更新失败和异常回滚契约。验证：`.venv/bin/pytest` 为 432 passed；覆盖率命令通过，总覆盖率 94.35%，`amz_template_management_service.py` 覆盖率 100%。

### [2026-05-05 16:15] [Codex]
继续加强测试覆盖：新增 `tests/unit/services/test_category_mapping_csv_updater.py`，固定品类映射 CSV 更新器的文件缺失、读取失败、空文件、校验错误、错误截断、部分更新告警和更新异常契约。验证：`.venv/bin/pytest` 为 439 passed；覆盖率命令通过，总覆盖率 94.78%，`category_mapping_csv_updater.py` 覆盖率 100%。

### [2026-05-05 16:17] [Codex]
继续加强测试覆盖：扩展 `tests/unit/services/test_listing_status_manager.py` 与 `tests/unit/services/test_amz_full_list_importer_service.py`，固定状态更新 0/-1 分支、CSV/TSV 读取、编码失败和清洗去重契约。验证：`.venv/bin/pytest` 为 444 passed；覆盖率命令通过，总覆盖率 95.13%，`ListingStatusManager` 与 `AmzFullListImporterService` 覆盖率 100%。

### [2026-05-05 16:20] [Codex]
继续加强测试覆盖：扩展 `tests/unit/utils/test_data_mapping_helpers_extracted.py`，固定单字段映射器的 item dimension、category lookup、未知 source type、JSON path、未知单位和 item weight 兜底契约。验证：`.venv/bin/pytest` 为 446 passed；覆盖率命令通过，总覆盖率 95.32%，`data_field_mapper.py` 覆盖率 100%。

### [2026-05-05 16:23] [Codex]
继续缓解 CI 规范偏移：GitHub Actions 日志确认 repo-level runner `amz-listing-runner-01` 当前版本为 `2.333.1`，满足官方 checkout v6 对 self-hosted runner `2.329.0+` 的要求；已将 `.github/workflows/ci.yml` 从 `actions/checkout@v4` 升级到 `actions/checkout@v6`，用于消除 Node.js 20 runtime deprecation。

### [2026-05-05 16:55] [Codex]
生产部署前迁移校验发现 Alembic 初始迁移未覆盖运行时依赖的 `ds_api_product_details`、`product_final_prices`、`amazon_cat_templates`，且历史 SQL 存在宿主机 owner 语句和 `supplier_categories_map` 列差异。已在 `alembic/versions/001_initial_schema.py` 补齐建表顺序、owner 过滤和兼容列，使用共享 PostgreSQL 临时库验证 `alembic upgrade head` 成功并确认关键表齐全。验证：`.venv/bin/pytest` 为 446 passed；覆盖率命令通过，总覆盖率 95.32%。
