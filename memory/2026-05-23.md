### [2026-05-23 12:35] [Codex]
只读巡检亚马逊自动发品仓库：源码 `dev` 分支当前在 `5682a1a`，包含 2026-05-18 的 OTTOMAN/SP-API 覆盖改造（如 `config/amz_listing_data_mapping/sp_api_ottoman.json`、`src/services/product_content_generator.py`、`src/services/content_validator.py`），但生产容器仍运行镜像 `amz-listing-management-system:2026-05-17`，容器内缺少上述文件。生产服务健康，`/api/readiness` 返回 200，支持发品类仍为 `CABINET` / `HOME_MIRROR`；待发统计为 CABINET 15、HOME_MIRROR 21、其他 85。后续若要让 OTTOMAN/5月18日改造进入生产，需要按生产部署流程重建并更新镜像 tag。

### [2026-05-23 12:55] [Codex]
调研并用生产 Amazon SP-API 凭据只读验证 listing 不合规/质量问题可获取性：`searchListingsItems` 带 `includedData=summaries,issues` 与 `withIssueSeverity=ERROR,WARNING` 可返回现有 listing issues，例如 SKU `meow251115Gb5dN` 返回 `ERROR 18503 / QUALIFICATION_REQUIRED`（Pesticides and Pesticide Devices 资质要求），多个 SKU 返回 `WARNING 18448 / MISSING_ATTRIBUTE`（缺少 `recommended_uses_for_product`）。`getListingsItem` 单 SKU 带 `includedData=summaries,issues,attributes,productTypes` 可拿同类 issue 明细。Reports API `GET_MERCHANTS_LISTINGS_FYP_REPORT` 可批量拉 Search Suppressed listing，验证 report `243988020596` 返回 1 条：SKU `AA000120240623` / ASIN `B0D8H4ZX2J`，Reason `Invalid information`，Issue Description 指出主图疑似包含文字/logo/图形/水印。结论：官方 API 能覆盖 Listings Items issue、suppressed listing 原因/处理说明、restriction reason；当前代码已具备基础 Listings/Reports client，但缺少 searchListingsItems、includedData issues 参数、FYP report 和 restrictions 封装与入库同步。

### [2026-05-23 14:20] [Codex]
实现 Listing Issue 运营监控与修复编排模块：新增 Alembic `003_amazon_listing_issues` 和 `amazon_listing_issues` / `amazon_listing_issue_actions` / scan run 表；`AmazonListingsClient` 支持 `includedData` 与 `searchListingsItems`，`AmazonReportsClient` 支持 `GET_MERCHANTS_LISTINGS_FYP_REPORT`；新增 `AmazonListingIssueSyncService` 同步 Listings Items issues 与 FYP suppressed report 并幂等入库；新增 `AmazonListingIssueRepairService` 将 `recommended_uses_for_product` 缺失转为可 dry-run/live 的安全 PATCH，将图片违规、pesticide/资质类和未知问题转为人工动作；新增 `sync-listing-issues` CLI 与可选 server 后台定时器（默认关闭，dry-run 默认开启）。验证：新增/受影响测试 29 passed，完整 `pytest -q` 542 tests passed，`alembic upgrade head --sql` 通过。部署前仍需将 Product Type Definitions schema S3 域名加入 ECS `amazon-spapi-proxy.service` allowlist，并先以 dry-run 复核 action 队列。

### [2026-05-23 14:45] [Codex]
基于当前 Amazon listing issues 复核发品流程优化点：`searchListingsItems` 返回 `numberOfResults=259`，样本中主要问题为 `18448/MISSING_ATTRIBUTE recommended_uses_for_product`、`18503/QUALIFICATION_REQUIRED`、`100335/INVALID_ATTRIBUTE item_depth_width_height.width > 42in`。FYP report 另有主图文字/logo/水印 search suppression。回溯典型 SKU：`meow251115Gb5dN` 源 Giga 描述含复杂 HTML/图片，已生成文案中曾出现 `bacteria buildup` 触发 pesticide 类资质风险；`meow251108bCYJ3` 为 60" bathroom vanity，源数据 `assembledLength=60.00`，现有 `amz_mapping.json` 将 assembledLength 映射为 `Item Width Side To Side`，再由 `sp_api_cabinet.json` 写入 `item_depth_width_height.width`，导致 CABINET schema 最大 42in 的质量警告。优化方向：发品前增加 schema 深度校验（required/enum/range）、合规敏感 claim 扫描、图片质量队列、按 issue 回灌模板字段/映射规则、对超出 product type schema 范围的商品阻断或进入人工品类复核。

### [2026-05-23 13:39] [Codex]
实现发品前 Listing 质量闸门与 issue 预防优化：新增 `AmazonListingQualityGate`，在 `AmazonListingSubmitter` 提交 Listings Items API 前自动补充 `recommended_uses_for_product`，并阻断 pesticide/device 高风险文案 claim、主图缺失/非 HTTPS、CABINET 已观测尺寸越界、已缓存 schema 必填缺失；Giga/B2B 供应商主图默认记录人工复核 warning，可通过 `LISTING_QUALITY_REQUIRE_IMAGE_REVIEW=true` 升级为阻断。`content_validator` 同步扩展 title / description / bullets / search_terms 的 pesticide/device claim warning。验证：新增/受影响测试 23 passed，完整 `pytest -q` 551 tests passed，`alembic upgrade head --sql` 通过。

### [2026-05-23 15:37] [Codex]
按最新进展补齐文档：`docs/architecture.md` 增加 Amazon listing issue 同步/修复、质量闸门和数据流；`docs/sp-api-integration-plan.md` 更新 Phase 3/4/4.1 状态、listing issue 监控验收、实际新增 issue 表和 Product Type Definitions S3 allowlist 风险；`/data/README.md` 和 `/data/TODO.md` 登记 ECS `amazon-spapi-proxy.service` 需补充 `selling-partner-definitions-prod-iad.s3.amazonaws.com:443` 的生产前置项。未新增端口或生产服务。
