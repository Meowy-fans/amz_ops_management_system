### [2026-05-17 18:02] [Codex]
整理 Amazon SP-API 接入规划：用户已提交 Seller Central 私有开发者 / 自用 API 权限申请，当前等待 Amazon 审核。新增 `docs/sp-api-integration-plan.md`，记录 Product Listing / Inventory and Order Tracking 最小权限、Giga 与内部系统数据用途、Amazon 信息不对外共享、上海阿里云 ECS 固定出口方案、Amazon API 基础设施、Reports API 只读同步、价格库存 patch、新品发品 API 出口、JSON_LISTINGS_FEED 批量提交、submission/issues/schema 建议表，以及风险和审核通过后的下一步。同步更新 `STATUS.md` 下一步计划和 `TODO.md` TASK-094~TASK-099。

### [2026-05-17 18:24] [Codex]
用户明确要求 Amazon API 交互固定通过堡垒机 / 上海阿里云 ECS 出口。已将 `docs/sp-api-integration-plan.md` 的网络方案升级为硬性要求：生产环境禁止 Amazon API 隐式直连，Amazon client 必须显式使用 `AMAZON_HTTPS_PROXY`，缺少堡垒机代理配置时 fail closed，写操作前需 smoke 验证观测出口 IP 为堡垒机/ECS。同步更新 `STATUS.md` 风险与下一步，并在 `TODO.md` 增加 TASK-100 配置 Amazon SP-API 堡垒机固定出口。

### [2026-05-17 19:04] [Codex]
开始执行 Amazon SP-API 后续计划：用户确认权限已获批并填入 `.env.amazon-sp-api`。在阿里云上海 ECS 部署 `amazon-spapi-proxy.service`（Python HTTP CONNECT，监听 `100.85.252.67:18080`，仅允许家用服务器 `100.95.123.106`，仅允许 `api.amazon.com:443` / `sellingpartnerapi-na.amazon.com:443`），生产 compose 已加载 `.env.amazon-sp-api` 并重建容器，容器 healthy。新增 `infrastructure/amazon/`：`AmazonConfig`、`AmazonTokenManager`、`AmazonAPIClient`，生产缺少 `AMAZON_HTTPS_PROXY` 时 fail closed，所有 Amazon 请求显式使用堡垒机代理。验证：代理访问 LWA 目标 CONNECT 成功，SP-API NA 返回 200，非 Amazon `example.com` 被 403；LWA token smoke 通过且未输出 token；Amazon infra 单测 7 passed。

### [2026-05-17 19:23] [Codex]
完成 Amazon Reports API Phase 2 只读同步：新增 `AmazonReportsClient`，支持 create report、轮询 report、get report document、经堡垒机代理下载报告文档并处理 GZIP；`AmzFullListImporterService` 新增 `sync_report_from_api()`，复用现有清洗和 `AmzFullListReportRepository.upsert_from_dataframe()`，保留手动文件导入 fallback；新增 CLI 任务 `sync-amz-report-api`。真实生产同步读取 418 行并入库，统计总记录 445、Active 254、唯一 ASIN 442。期间发现 report document 下载域名为 `tortuga-prod-na.s3-external-1.amazonaws.com`，已加入 ECS `amazon-spapi-proxy.service` allowlist。生产镜像升级为 `amz-listing-management-system:2026-05-17`，容器 healthy；`pytest -q` 通过。

### [2026-05-17 23:59] [Codex]
完成 Phase A-E 多源标准化发品流程改造（510 tests pass）：

**Phase A — 标准化产品模型**：新增 `src/models/product.py`（`StandardProduct` + 规格 data class）、`src/services/product_normalizer.py`（`GigaProductNormalizer` + 注册表，Giga raw_data → StandardProduct，含单位转换），16 个单测。

**Phase B — 品类自动发现**：新增 CLI 任务 `discover-product-type`（交互式搜索 Amazon product type + 展示必填字段 + 写入映射）和 `suggest-category-mappings`（扫描未映射 Giga 品类，用商品名搜索 Amazon 候选 product type），已验证 `suggest-category-mappings` 成功匹配 "Kids Bikes" → `RIDE_ON_TOY`。

**Phase C — Schema 集成**：新建 `amazon_product_type_schemas` 缓存表，`AmazonSchemaService`（DB 优先缓取 + API fallback、本地必填校验、合法值提取、属性描述查询），集成到 `AmazonListingSubmitter._pre_validate()` 提交前预检，8 个单测。

**Phase D — LLM Pipeline 增强**：新增 `prod_detail_gen_v2` 品类感知 prompt（注入 category context + Amazon style rules + buyer persona + valid values hints），`ProductContentGenerator`（StandardProduct → EnrichedProductContent，单次 LLM 调用产出 title/bullets/description/search_terms/generic_keyword + 属性增强），后验证层（长度/禁止词/HTML 安全/ALL CAPS），9 个单测。

**Phase E — 变体 API 提交**：`sp_api_common.json` 补充变体映射（parentage 类型 + child_parent_sku_relationship 后处理）。

**规范偏移修复**：红线级抽取 `AmazonProductTypeSchemaRepository`、拆分 `_convert()`（104→5 个 group handler）、拆分 `product_content_generator.py`（320→284 行）+ 提取 `content_validator.py`、`amazon_attribute_mapper.py`（318→298 行）、修复死 logger/export/类型注解。`pytest -q` 510 passed。
